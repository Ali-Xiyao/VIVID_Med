# VIVID-Med：数据与实验

> 最后更新：2026-02-20（V12 重构版，SPD only）

## 1. 数据集总览

| 数据集 | 模态 | 用途 | 样本量 | 标签 |
| --- | --- | --- | --- | --- |
| CheXpert 30k | CXR | 预训练 + 分类 transfer | 30,000 | 12 findings（去掉 No Finding + Pleural Other） |
| NIH ChestX-ray14 | CXR | External test | 25,596 | 8 共同标签（跨域评估） |

### 1.1 CheXpert

- 来源：CheXpert-v1.0-small（Stanford）
- 12-label 口径：Enlarged Cardiomediastinum, Cardiomegaly, Lung Opacity, Lung Lesion, Edema, Consolidation, Pneumonia, Atelectasis, Pneumothorax, Pleural Effusion, Fracture, Support Devices
- 去掉 No Finding（全阳性，AUC=NaN）和 Pleural Other（极低频 <50 样本）
- 标签处理：blank/NaN → state=null + answerability=false；uncertain → state=uncertain
- UMS 文件：`data/dataset/processed/chexpert_ums_train.jsonl`

### 1.2 NIH ChestX-ray14

- 来源：NIH Clinical Center
- 仅用于 external test（不参与训练）
- 8 个共同标签：No Finding, Atelectasis, Cardiomegaly, Consolidation, Edema, Pleural Effusion, Pneumonia, Pneumothorax

## 2. 主方法配置

主方法 = SPD(3组×2tokens, ortho=0.01) + 12-label + field_query_training + answerability_mask

| 参数 | 值 |
| --- | --- |
| SPD | num_groups=3, tokens_per_group=2, ortho_weight=0.01 |
| 预训练标签 | 12-label（去掉 No Finding + Pleural Other） |
| field_query_training | k_min=4, k_max=6, focus_probability=0.6 |
| answerability_mask | true_weight=1.0, false_weight=0.0 |
| LLM | Qwen2.5-1.5B-Instruct, 冻结 |
| ViT | vit_base_patch16_224, vit_lr=2e-5 |
| 训练 | batch=4, grad_accum=8, max_steps=10000, bf16 |

> 注：HFP（多层融合）在旧 V12 中曾与 SPD 联合使用，但历史数据显示 SPD+HFP 反而低于 SPD 单独（LP AUC 0.7930 vs 0.8441），已排除。

---

## 3. 实验设计（全部待重跑）

### 3.1 对比实验（主表 Table 1）

以 SPD 为主方法，Linear Probe 评估（冻结 ViT，12-label 口径）。

| # | 实验 | 说明 | 证明什么 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | Baseline (ImageNet supervised) | ImageNet 预训练 ViT，直接 LP | 通用监督预训练基线 | ✅ seed=0（12-label F1=0.8845, AUC=0.7523） |
| 2 | Random mask (VRL 风格 75%) | frozen LLM + random 75% token drop | ViTP 风格 proxy | ⬜ 需 12-label 重跑（旧版 7-label） |
| 3 | 纯框架（= 消融链 A+UMS） | frozen LLM + MLP projector + UMS JSON，无 SPD | 框架基线 | ✅ seed=0（F1=0.9095, 12L-AUC=0.8461） |
| 4 | **SPD（ours）**（= 消融链 A+UMS+SPD） | frozen LLM + SPD + 12-label | 完整方法 | ✅ seed=0（F1=0.9114, 12L-AUC=0.8564） |

> 注意：所有对比实验统一 12-label 预训练口径，统一评估协议。
> 纯框架与消融链 A+UMS 等价（frozen LLM + MLP projector + UMS JSON，无 SPD），直接复用 A+UMS 数据，无需单独预训练。

### 3.2 消融实验（Table 3 消融递进链）

每步只加一个变量，形成干净的单变量递进链。

| # | 实验 | 监督格式 | SPD | 证明什么 | 状态 |
| --- | --- | --- | --- | --- | --- |
| 1 | A（free-text, 随机句式v2） | 自由文本 | ✗ | frozen LLM + 自由文本的基线 | ✅ seed=0（F1=0.9064, 12L-AUC=0.8214） |
| 2 | A+UMS（JSON） | 结构化 JSON | ✗ | UMS 结构化监督 vs 自由文本 | ✅ seed=0（F1=0.9095, 12L-AUC=0.8461） |
| 3 | **A+UMS+SPD** | 结构化 JSON | ✓ | SPD 的独立贡献 | ✅ seed=0（F1=0.9114, 12L-AUC=0.8564） |

递进链回答的问题：

- A → A+UMS：结构化 JSON 监督 vs 自由文本，是否有增益？
- A+UMS → A+UMS+SPD：SPD 在 UMS 基础上是否有独立增益？

### 3.3 额外消融（Table 3 扩展行）

| # | 实验 | 说明 | 证明什么 | 状态 |
| --- | --- | --- | --- | --- |
| 4 | Q-Former proxy | 单组 SPD（num_groups=1, tokens=6, ortho=0） | 正交分解 vs 普通 cross-attention | ⬜ 待跑 |
| 5 | Frozen vs Trainable LLM | 解冻 LLM（lr=1e-5） | 冻结 LLM 的必要性 | ⬜ 待跑 |

### 3.4 跨域泛化（Table 2）

用 CheXpert LP checkpoint 直接在 NIH ChestX-ray14 上测试（不重新训练）。

| # | 实验 | 状态 |
| --- | --- | --- |
| 1 | Baseline (ImageNet) | ⬜ 待跑 |
| 2 | 纯框架（= A+UMS） | ⬜ 待跑（LP 完成后） |
| 3 | **SPD（ours）** | ⬜ 待跑 |
| 4 | 消融递进链各组 | ⬜ 待跑 |

### 3.5 补充实验

| # | 实验 | 说明 | 优先级 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | Full Fine-tune | 解冻 ViT，12-label 口径 | P1 | ⬜ 待跑 |
| 2 | 多 seed（×3 seeds: 0/1/2） | mean±std，统计显著性 | P0 | ⬜ 待跑 |
| 3 | Low-data 曲线（10%/25%/50%） | 数据效率验证 | P1 | ⬜ 待跑 |
| 4 | LLM 规模对比（0.5B/1.5B/3B） | Scaling analysis | P2 | ⬜ 待跑 |

---

## 4. 评估协议

### 4.1 Linear Probe（主表评估方式）

冻结 ViT backbone，只训练线性分类头，直接衡量表征质量。

| 参数 | 值 |
| --- | --- |
| backbone_lr | 0（冻结 ViT） |
| head_lr | 1e-3 |
| weight_decay | 0 |
| max_steps | 3000 |
| eval_interval | 200 |
| drop_path_rate | 0.0 |
| batch_size | 16 |
| gradient_accumulation_steps | 2 |
| bf16 | true |

Checkpoint 选择规则：取 val_loss 最低的 step 报告所有指标。
指标口径：12-label F1 + 12-label AUC（去掉 No Finding 和 Pleural Other）。
3 seeds (0,1,2)，报告 mean ± std。

### 4.2 NIH 跨域评估

用 CheXpert LP checkpoint 直接在 NIH 上测试。AUC 为跨域核心指标（不受 threshold 影响）。

### 4.3 Full Fine-tune（补充）

解冻 backbone，backbone_lr=1e-5，head_lr=1e-3，max_steps=6000。作为补充报告。

---

## 5. 执行计划

### 5.1 Phase 1：SPD 预训练

```powershell
$env:CUDA_VISIBLE_DEVICES="0"; $env:HF_HUB_OFFLINE=1; $env:TRANSFORMERS_OFFLINE=1
python scripts/train_cxr.py --config configs/ablation_A_ums_spd_12label.yaml
```

### 5.2 Phase 2：对比实验预训练

需要新建的配置文件：

- `configs/v12_random_mask_12label.yaml` — frozen LLM + random 75% mask + 12-label

```powershell
$env:CUDA_VISIBLE_DEVICES="0"
python scripts/train_cxr.py --config configs/v12_random_mask_12label.yaml
```

> 纯框架基线直接复用消融链 A+UMS 的 checkpoint，无需单独预训练。

### 5.3 Phase 3：消融递进链预训练

- `configs/ablation_A_freetext_12label.yaml` — 已有
- `configs/ablation_A_ums_12label.yaml` — 已有
- `configs/ablation_A_ums_spd_12label.yaml` — 已有，即主方法

### 5.4 Phase 4：Linear Probe 评估（所有预训练完成后）

每个预训练 checkpoint × 3 seeds LP。

### 5.5 Phase 5：NIH 跨域评估

```powershell
python scripts/eval_nih_crossdomain.py --checkpoint <best.pt>
```
