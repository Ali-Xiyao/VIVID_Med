# VIVID-Med：数据与实验

> 最后更新：2026-02-23（V12 重构版，SPD only，CT 分类 LP 全部 12 组实验已完成）

## 1. 数据集总览

| 数据集 | 模态 | 用途 | 样本量 | 标签 |
| --- | --- | --- | --- | --- |
| CheXpert 30k | CXR | 预训练 + 分类 transfer | 30,000 | 12 findings（去掉 No Finding + Pleural Other） |
| NIH ChestX-ray14 | CXR | External test | 25,596 | 8 共同标签（跨域评估） |
| LIDC-IDRI | 胸部 CT | CT 分类 LP（同解剖跨模态） | 875 cases, 2,630 nodules | 二分类 (benign/malignant) |
| OrganAMNIST | 腹部 CT | CT 分类 LP（跨解剖跨模态） | 58,830 slices (train 34,561 / val 6,491 / test 17,778) | 11 器官分类 |

### 1.1 CheXpert

- 来源：CheXpert-v1.0-small（Stanford）
- 12-label 口径：Enlarged Cardiomediastinum, Cardiomegaly, Lung Opacity, Lung Lesion, Edema, Consolidation, Pneumonia, Atelectasis, Pneumothorax, Pleural Effusion, Fracture, Support Devices
- 去掉 No Finding（全阳性，AUC=NaN）和 Pleural Other（极低频 <50 样本）
- 标签处理：blank/NaN → state=null + answerability=false；uncertain → state=uncertain
- UMS 文件：`data/dataset/processed/chexpert_ums_train.jsonl`

### 1.2 NIH ChestX-ray14

- 来源：NIH Clinical Center
- 仅用于 external test（不参与训练）
- 8 个共同标签：No Finding, Atelectasis, Cardiomegaly, Consolidation, Edema, Pleural Effusion, Pneumonia, Pneumothorax

### 1.3 LIDC-IDRI

- 来源：LIDC-IDRI（Lung Image Database Consortium）
- 用途：CT 分类 LP（同解剖跨模态：CXR 预训练 → 胸部 CT 分类）
- 任务：肺结节良恶性二分类
- 数据量：875 cases, 2,630 nodules, 15,548 slices
- 标签：4 位放射科医生标注 malignancy score (1-5)，取平均值
  - ≤2.0 → benign (0), ≥4.0 → malignant (1), 中间值排除
- 每个 nodule 取中间 slice 作为代表
- Patient-level split 防止数据泄漏 (train 70% / val 20% / test 10%)
- 标签文件：`data/dataset/processed/lidc_nodule_labels.csv`（需先运行 `scripts/preprocess_lidc_labels.py`）

### 1.4 OrganAMNIST

- 来源：MedMNIST v2 (OrganAMNIST)
- 用途：CT 分类 LP（跨解剖跨模态：CXR 预训练 → 腹部 CT 分类）
- 任务：腹部 CT 器官 11 分类
- 数据量：train 34,561 / val 6,491 / test 17,778（共 58,830）
- 类别：bladder, femur_left, femur_right, heart, kidney_left, kidney_right, liver, lung_left, lung_right, spleen, stomach
- 数据格式：`organamnist_224.npz`（224×224 灰度 → RGB 复制三通道）

## 2. 主方法配置

主方法 = SPD(3组×2tokens, ortho=0.01) + 12-label + field_query_training + answerability_mask

| 参数 | 值 |
| --- | --- |
| SPD | num_groups=3, tokens_per_group=2, ortho_weight=0.01 |
| 预训练标签 | 12-label（去掉 No Finding + Pleural Other） |
| field_query_training | k_min=4, k_max=6, focus_probability=0.6 |
| answerability_mask | true_weight=1.0, false_weight=0.0 |
| LLM | Qwen2.5-1.5B-Instruct, 冻结 |
| ViT | vit_base_patch16_224, vit_lr=2e-5 |
| 训练 | batch=4, grad_accum=8, max_steps=10000, bf16 |

> 注：HFP（多层融合）在旧 V12 中曾与 SPD 联合使用，但历史数据显示 SPD+HFP 反而低于 SPD 单独（LP AUC 0.7930 vs 0.8441），已排除。

---

## 3. 实验设计

### 3.1 对比实验（主表 Table 1 — LP，CheXpert 12-label）

以 SPD 为主方法，Linear Probe 评估（冻结 ViT，12-label 口径），×3 seeds (0/1/2)。

| # | 实验 | 12-label F1 | 12-label AUC | 状态 |
| --- | --- | ---: | ---: | --- |
| 1 | Baseline (ImageNet supervised) | 0.8813±0.0046 | 0.7746±0.0081 | ✅ ×3 seeds |
| 2 | MAE (ImageNet self-supervised) | 0.8657±0.0096 | 0.7211±0.0031 | ✅ ×3 seeds |
| 3 | DINOv3 (self-supervised) | 0.8760±0.0014 | 0.6876±0.0053 | ✅ ×3 seeds |
| 4 | BiomedCLIP (Medical VL) | 0.8989±0.0050 | 0.7923±0.0249 | ✅ ×3 seeds |
| 5 | Random mask (VRL 风格 75%) | 0.8705±0.0156 | 0.7396±0.0020 | ✅ ×3 seeds |
| 6 | 纯框架（= 消融链 A+UMS） | 0.8963±0.0012 | 0.8431±0.0035 | ✅ ×3 seeds |
| 7 | **SPD（ours）**（= 消融链 A+UMS+SPD） | 0.9016±0.0060 | 0.8588±0.0033 | ✅ ×3 seeds |

> 纯框架与消融链 A+UMS 等价，直接复用 A+UMS 数据。

### 3.2 消融实验（Table 3 消融递进链）

每步只加一个变量，形成干净的单变量递进链。×3 seeds (0/1/2)。

| # | 实验 | 监督格式 | SPD | macro_F1 | 12-label AUC | 状态 |
| --- | --- | --- | --- | ---: | ---: | --- |
| 1 | A（free-text, 随机句式v2） | 自由文本 | ✗ | 0.8877±0.0027 | 0.8253±0.0028 | ✅ ×3 seeds |
| 2 | A+UMS（JSON） | 结构化 JSON | ✗ | 0.8963±0.0012 | 0.8431±0.0035 | ✅ ×3 seeds |
| 3 | **A+UMS+SPD** | 结构化 JSON | ✓ | 0.9016±0.0060 | 0.8588±0.0033 | ✅ ×3 seeds |

递进链论证（已验证）：

- A → A+UMS：12-label AUC +1.78pp（0.8253→0.8431）✅
- A+UMS → A+UMS+SPD：12-label AUC +1.57pp（0.8431→0.8588）✅
- 全链提升：12-label AUC +3.35pp（0.8253→0.8588）✅

### 3.3 额外消融（Table 3 扩展行）

| # | 实验 | 说明 | 证明什么 | 状态 |
| --- | --- | --- | --- | --- |
| 4 | Q-Former proxy | 单组 SPD（num_groups=1, tokens=8, ortho=0） | 正交分解 vs 普通 cross-attention | ✅ ×3 seeds（F1=0.9167±0.0016/AUC=0.8182±0.0195） |

### 3.4 跨域泛化（Table 2 — NIH 8 共同标签）

用 CheXpert LP checkpoint 直接在 NIH ChestX-ray14 上测试（不重新训练）。

| # | 实验 | macro_AUC | macro_F1 | 状态 |
| --- | --- | ---: | ---: | --- |
| 1 | Baseline (ImageNet supervised) | 0.6147±0.0012 | 0.2090±0.0111 | ✅ ×3 seeds |
| 2 | MAE (ImageNet self-supervised) | 0.5945±0.0013 | 0.1847±0.0005 | ✅ ×3 seeds |
| 3 | DINOv3 (self-supervised) | 0.6287±0.0023 | 0.2049±0.0050 | ✅ ×3 seeds |
| 4 | BiomedCLIP (Medical VL) | 0.6725±0.0076 | 0.2246±0.0058 | ✅ ×3 seeds |
| 5 | Random mask (VRL 风格 75%) | 0.6000±0.0018 | 0.1969±0.0031 | ✅ ×3 seeds |
| 6 | 纯框架（= A+UMS） | 0.7014±0.0048 | 0.2472±0.0025 | ✅ ×3 seeds |
| 7 | **SPD（ours）**（= A+UMS+SPD） | 0.7225±0.0019 | 0.2621±0.0010 | ✅ ×3 seeds |

> Baseline 各 seed 详情：seed=0 AUC=0.6146/F1=0.2218，seed=1 AUC=0.6159/F1=0.2033，seed=2 AUC=0.6135/F1=0.2019。
> Random mask 各 seed 详情：seed=0 AUC=0.6002/F1=0.1940，seed=1 AUC=0.6017/F1=0.1965，seed=2 AUC=0.5982/F1=0.2001。
> MAE 各 seed 详情：seed=0 AUC=0.5939/F1=0.1850，seed=1 AUC=0.5960/F1=0.1841，seed=2 AUC=0.5936/F1=0.1850。
> BiomedCLIP 各 seed 详情：seed=0 AUC=0.6730/F1=0.2235，seed=1 AUC=0.6799/F1=0.2194，seed=2 AUC=0.6647/F1=0.2308。
> A+UMS 各 seed 详情：seed=0 AUC=0.7068/F1=0.2453，seed=1 AUC=0.6977/F1=0.2500，seed=2 AUC=0.6996/F1=0.2463。
> A+UMS+SPD 各 seed 详情：seed=0 AUC=0.7214/F1=0.2620，seed=1 AUC=0.7247/F1=0.2631，seed=2 AUC=0.7215/F1=0.2612。
>
> NIH 跨域 AUC 排序：MAE(0.5945) < Random mask(0.6000) < Baseline(0.6147) < DINOv3(0.6287) < BiomedCLIP(0.6725) < A+UMS(0.7014) < SPD(0.7225)。
> SPD vs DINOv3：+9.38pp（0.6287→0.7225），SPD vs BiomedCLIP：+5.00pp（0.6725→0.7225），SPD vs Baseline：+10.78pp（0.6147→0.7225）。
> SPD vs Random mask：+12.25pp（0.6000→0.7225）。

### 3.5 补充实验

| # | 实验 | 说明 | 优先级 | 状态 |
| --- | --- | --- | --- | --- |
| 1 | Low-data 曲线（10%/25%/50%） | 数据效率验证，可能不利于方法叙事 | P3 | ⬜ 搁置 |
| 2 | LLM 规模对比（0.5B/1.5B/3B） | Scaling analysis，暂不跑 | P3 | ⬜ 搁置 |
| 3 | MAE baseline | Table 1 + NIH | P2 | ✅ 已完成 |
| 4 | BiomedCLIP baseline | Table 1 + NIH | P2 | ✅ 已完成 |

### 3.6 LIDC-IDRI 肺结节分类 LP（Table 4a — 同解剖跨模态）

CXR 预训练 → 胸部 CT 肺结节良恶性二分类。冻结 ViT backbone，只训练线性分类头。

| # | 实验 | ViT 初始化 | Config | Accuracy | macro_F1 | AUC | 状态 |
| --- | --- | --- | --- | ---: | ---: | ---: | --- |
| 1 | ImageNet supervised | timm pretrained | `ct_lp_lidc_imagenet.yaml` | 0.7429 | 0.6406 | 0.7542 | ✅ 完成 |
| 2 | BiomedCLIP | `pretrained/biomedclip_vit_base.pt` | `ct_lp_lidc_biomedclip.yaml` | 0.7905 | 0.7188 | 0.8261 | ✅ 完成 |
| 3 | MAE | `pretrained/mae_pretrain_vit_base.pth` | `ct_lp_lidc_mae.yaml` | 0.7619 | 0.5506 | 0.7964 | ✅ 完成 |
| 4 | DINOv3 | `pretrained/dinov3_vit_base.pt` | `ct_lp_lidc_dinov3.yaml` | 0.7333 | 0.4231 | 0.7579 | ✅ 完成 |
| 5 | 纯框架（A+UMS） | `ablation_A_ums_12label/best.pt` | `ct_lp_lidc_a_ums.yaml` | 0.8476 | 0.7955 | 0.8622 | ✅ 完成 |
| 6 | **SPD（ours）** | `ablation_A_ums_spd_12label/best.pt` | `ct_lp_lidc_spd.yaml` | 0.8571 | 0.8004 | 0.8400 | ✅ 完成 |

### 3.7 OrganAMNIST 器官分类 LP（Table 4b — 跨解剖跨模态）

CXR 预训练 → 腹部 CT 器官 11 分类。冻结 ViT backbone，只训练线性分类头。

| # | 实验 | ViT 初始化 | Config | Accuracy | macro_F1 | AUC | 状态 |
| --- | --- | --- | --- | ---: | ---: | ---: | --- |
| 1 | ImageNet supervised | timm pretrained | `ct_lp_organ_imagenet.yaml` | 0.8932 | 0.8840 | 0.9928 | ✅ 完成 |
| 2 | BiomedCLIP | `pretrained/biomedclip_vit_base.pt` | `ct_lp_organ_biomedclip.yaml` | 0.8763 | 0.8661 | 0.9912 | ✅ 完成 |
| 3 | MAE | `pretrained/mae_pretrain_vit_base.pth` | `ct_lp_organ_mae.yaml` | 0.8090 | 0.7760 | 0.9824 | ✅ 完成 |
| 4 | DINOv3 | `pretrained/dinov3_vit_base.pt` | `ct_lp_organ_dinov3.yaml` | 0.5349 | 0.4512 | 0.9234 | ✅ 完成 |
| 5 | 纯框架（A+UMS） | `ablation_A_ums_12label/best.pt` | `ct_lp_organ_a_ums.yaml` | 0.9157 | 0.9043 | 0.9948 | ✅ 完成 |
| 6 | **SPD（ours）** | `ablation_A_ums_spd_12label/best.pt` | `ct_lp_organ_spd.yaml` | 0.9370 | 0.9325 | 0.9969 | ✅ 完成 |

运行命令（逐个执行）：

```bash
# LIDC-IDRI (需先生成标签: pip install pylidc && python scripts/preprocess_lidc_labels.py)
python scripts/train_ct_lp.py --config configs/ct_lp_lidc_imagenet.yaml
python scripts/train_ct_lp.py --config configs/ct_lp_lidc_biomedclip.yaml
python scripts/train_ct_lp.py --config configs/ct_lp_lidc_a_ums.yaml
python scripts/train_ct_lp.py --config configs/ct_lp_lidc_spd.yaml

# OrganAMNIST
python scripts/train_ct_lp.py --config configs/ct_lp_organ_imagenet.yaml
python scripts/train_ct_lp.py --config configs/ct_lp_organ_biomedclip.yaml
python scripts/train_ct_lp.py --config configs/ct_lp_organ_a_ums.yaml
python scripts/train_ct_lp.py --config configs/ct_lp_organ_spd.yaml
```

---

## 4. 评估协议

### 4.1 Linear Probe（主表评估方式）

冻结 ViT backbone，只训练线性分类头，直接衡量表征质量。

| 参数 | 值 |
| --- | --- |
| backbone_lr | 0（冻结 ViT） |
| head_lr | 1e-3 |
| weight_decay | 0 |
| max_steps | 3000 |
| eval_interval | 200 |
| drop_path_rate | 0.0 |
| batch_size | 16 |
| gradient_accumulation_steps | 2 |
| bf16 | true |

Checkpoint 选择规则：取 val_loss 最低的 step 报告所有指标。
指标口径：12-label F1 + 12-label AUC（去掉 No Finding 和 Pleural Other）。
3 seeds (0,1,2)，报告 mean ± std。

### 4.2 NIH 跨域评估

用 CheXpert LP checkpoint 直接在 NIH 上测试（不重新训练）。AUC 为跨域核心指标（不受 threshold 影响）。8 共同标签，25,596 样本。

### 4.3 CT 分类 Linear Probe 评估

CXR 预训练 ViT → CT 切片分类。冻结 ViT backbone，只训练线性分类头（与 CheXpert LP 协议一致）。

| 参数 | 值 |
| --- | --- |
| backbone_lr | 0（冻结 ViT） |
| head_lr | 1e-3 |
| weight_decay | 0 |
| max_steps | 3,000 |
| warmup_steps | 200 |
| eval_interval | 200 |
| batch_size | 16 |
| gradient_accumulation_steps | 2 |
| bf16 | true |

LIDC-IDRI：二分类 (benign/malignant)，CrossEntropyLoss，指标 Accuracy / macro_F1 / AUC。
OrganAMNIST：11 分类，CrossEntropyLoss，指标 Accuracy / macro_F1 / macro_AUC (OVR)。
Checkpoint 选择规则：取 val_loss 最低的 step。

---

## 5. 执行计划

### 5.1 消融递进链预训练（✅ 已完成）

- `configs/ablation_A_freetext_12label.yaml` — A（free-text）
- `configs/ablation_A_ums_12label.yaml` — A+UMS（JSON）
- `configs/ablation_A_ums_spd_12label.yaml` — A+UMS+SPD（主方法）

### 5.2 Linear Probe ×3 seeds（✅ 已完成）

已加 `--seed` 参数支持（`train_vit_baseline.py` / `train_cxr.py`），传入后覆盖 config 中的 seed 值，并自动在 output_dir 后追加 `_seed{N}` 后缀。

LP configs：`lp_imagenet_baseline.yaml` / `lp_A_freetext_12label.yaml` / `lp_A_ums_12label.yaml` / `lp_A_ums_spd_12label.yaml` / `lp_mae_baseline.yaml` / `lp_biomedclip_baseline.yaml` / `lp_random_mask_12label.yaml` / `lp_qformer_proxy_12label.yaml`

### 5.3 NIH 跨域评估（✅ 已完成）

```powershell
python scripts/eval_nih_crossdomain.py --checkpoint <best.pt>
```

### 5.4 对比实验（✅ 已完成）

- Random Mask 12-label 预训练 + LP ×3 seeds ✅
- Q-Former Proxy 预训练 + LP ×3 seeds ✅
- MAE baseline LP ×3 seeds + NIH ×3 seeds ✅
- BiomedCLIP baseline LP ×3 seeds + NIH ×3 seeds ✅
- DINOv3 baseline LP ×3 seeds + NIH ×3 seeds ✅

### 5.5 CT 分类 Linear Probe（⬜ 进行中）

前置步骤：LIDC 需先生成标签文件

```bash
pip install pylidc
python scripts/preprocess_lidc_labels.py
```

LIDC-IDRI（同解剖跨模态）：

- `configs/ct_lp_lidc_imagenet.yaml` — ImageNet supervised baseline ⬜
- `configs/ct_lp_lidc_biomedclip.yaml` — BiomedCLIP baseline ⬜
- `configs/ct_lp_lidc_a_ums.yaml` — A+UMS（纯框架） ⬜
- `configs/ct_lp_lidc_spd.yaml` — SPD（ours） ⬜

OrganAMNIST（跨解剖跨模态）：

- `configs/ct_lp_organ_imagenet.yaml` — ImageNet supervised baseline ⬜
- `configs/ct_lp_organ_biomedclip.yaml` — BiomedCLIP baseline ⬜
- `configs/ct_lp_organ_a_ums.yaml` — A+UMS（纯框架） ⬜
- `configs/ct_lp_organ_spd.yaml` — SPD（ours） ⬜

---

## 6. 可视化分析

所有可视化输出已保存至 `profile/paper/`。

### 6.1 Per-class AUC 对比（SPD vs Q-Former proxy）

文件：`per_class_auc_spd_vs_qformer.png`

SPD 与 Q-Former proxy 在 12 个标签上的 AUC 对比，解释 F1/AUC dissociation 现象：

- Q-Former proxy 的 macro F1（0.9167）高于 SPD（0.9016），但 macro AUC（0.8182）低于 SPD（0.8588）
- 原因：Q-Former 在高频标签（如 Lung Opacity）上表现好（主导 F1），但在低频标签上显著弱于 SPD（拉低 macro AUC）
- SPD 优势标签（低频）：Pleural Other +18pp、Pneumonia +3.9pp、Lung Lesion +3.6pp
- Q-Former 优势标签（高频）：Lung Opacity -4.0pp
- 结论：SPD 的正交分解使表征对低频/困难标签更敏感，macro AUC 更能反映真实表征质量

### 6.2 t-SNE 可视化（CLS Embeddings 对比）

文件：`tsne_comparison.png`

对比 ImageNet supervised / BiomedCLIP / VIVID-Med (SPD) 三个 ViT 的 CLS embedding 分布：

- 使用 CheXpert val set（最多 2000 样本），按 dominant finding 着色
- 可观察不同预训练方法对 CLS 表征空间结构的影响
- 脚本：`scripts/plot_tsne.py`

### 6.3 SPD Attention Map 可视化

文件：`attn_sample_0~7.png`，`summary.json`

展示 SPD 4 组 query tokens 的 cross-attention 热图，验证正交分解有效性：

- 每组 query tokens 关注不同的解剖区域，视觉上可明显区分
- 正交性量化（cosine similarity between groups）：

| 组对 | Cosine Similarity |
| --- | ---: |
| G1-G2 | 1.67e-05 |
| G1-G3 | 4.72e-07 |
| G1-G4 | 1.62e-09 |
| G2-G3 | 1.65e-08 |
| G2-G4 | 2.54e-07 |
| G3-G4 | 6.41e-06 |

- 所有组对的 cosine similarity 均 < 1e-4，接近完全正交
- 证明 ortho_loss 有效约束了各组 query tokens 关注不同语义方面
- 脚本：`scripts/plot_attention_maps.py`（使用预训练 checkpoint `outputs/ablation_A_ums_spd_12label/checkpoints/best.pt`）
