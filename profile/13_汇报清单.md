# 13 汇报清单（更新至 2026-02-12）

## 1. 项目目标（一句话）
- 用冻结 `LLM` 的结构化监督信号训练 `ViT + Projector`，让模型对 CXR 图像做可验证的结构化判别，而不是只会输出模板化 `null`。

## 2. 已完成内容（到今天）
- 完成 `V4 -> V4.2 -> V6 Top7` 的连续实验迭代，已从“全空塌缩”阶段脱离。
- 完成 `Top7` 字段聚焦训练，减少长尾字段对训练稳定性的破坏。
- 完成两条并行实验：
- 主线：`configs/cxr_chexpert_v6_top7_gpu0.yaml`
- 抗塌缩对照：`configs/cxr_chexpert_v6_top7_gpu1_abstain.yaml`
- 两条都已训练结束并完成评估，结果文件已落盘。

## 3. 当前核心问题（精确定义）
- 不是“JSON 生成失败”，而是“标签判别仍不平衡”。
- 不是“完全没数据”，而是“字段覆盖极不均衡，头部字段可学、部分字段召回低”。
- `abstain` 策略会抬高指标观感，但会降低有效评估覆盖率，不能作为唯一主结果。

## 4. 最新双实验结果（本次补录）
### 4.1 训练过程一眼结果
- `GPU0` 主线：`outputs/cxr_chexpert_v6_top7_gpu0_run1`
- 完成步数：`4500/4500`
- 最优验证损失：`0.0706 @ step 2400`
- 最终验证损失：`0.0755 @ step 4500`
- 关键权重：`checkpoints/best.pt`、`checkpoints/final.pt`
- `GPU1` 对照：`outputs/cxr_chexpert_v6_top7_gpu1_abstain_run1`
- 完成步数：`1200/1200`
- 最优验证损失：`0.0918 @ step 1080`
- 最终验证损失：`0.0928 @ step 1200`
- 关键权重：`checkpoints/best.pt`、`checkpoints/final.pt`

### 4.2 评估指标（1000 样本）
| 实验 | json_success_rate | pred_nan_rate | pred_unknown_policy | eval_coverage | macro_f1 | micro_f1 |
|---|---:|---:|---|---:|---:|---:|
| V6 Top7 GPU0 Run1 | 1.0 | 0.3814 | negative | 1.0000 | 0.4087 | 0.5996 |
| V6 Top7 GPU1 Abstain Run1 | 1.0 | 0.2526 | abstain | 0.4962 | 0.6357 | 0.8806 |

### 4.3 正确解读（汇报必须讲清）
- 两条实验都不再是 `pred_nan_rate=1.0`，说明“全空塌缩”已被打破。
- `GPU0` 是“必须给答案”口径（覆盖率 100%），更接近真实部署与公平对比。
- `GPU1` 是“允许弃答”口径（覆盖率 49.62%），指标高但过滤了大量样本，不能直接和 `GPU0` 比高低。
- 目前主线建议使用 `GPU0` 结果继续优化，`GPU1` 保留为诊断/上限参考。

## 5. Baseline 训练方式（详细口径）
以下是当前 baseline 的完整训练定义，用于和 VIVID 公平对比：

### 5.1 配置与脚本
- 配置：`configs/baseline_vit_chexpert_top7_dense10k.yaml`
- 训练脚本：`scripts/train_vit_baseline.py`
- 评估脚本：`scripts/eval_vit_baseline.py`

### 5.2 数据口径
- 数据源：`chexpert_ums_train.jsonl` / `chexpert_ums_val.jsonl`
- 标签集合：固定 `Top7` 字段（与 V6 一致）
- 采样策略：`train_dense_top_k=10000`，`val_dense_top_k=1000`
- 不确定标签策略：`uncertain_policy=ignore`（训练与评估都可忽略 -1/NaN）

### 5.3 模型结构
- 主干：`vit_base_patch16_224`
- 初始化：`vit_pretrained: true`（ImageNet 预训练，不是从零开始）
- 头部：直接多标签分类头（输出 7 维 logits）
- 与 VIVID 差异：baseline 不经过 `LLM`、不做 JSON 生成、不用 prompt。

### 5.4 损失函数与优化
- 损失：`BCEWithLogitsLoss(reduction="none") + mask`
- 只对可监督位置计算 loss（NaN/忽略位不反传）
- 优化器：`AdamW`
- 学习率策略：`Linear Warmup + CosineAnnealing`
- 典型训练参数：
- `lr=1e-4`
- `batch_size=16`
- `grad_acc=2`
- `max_steps=4500`（Top7 dense10k 配置）

### 5.5 评估方式
- 阈值：`0.5`
- 输出：`macro_f1`、`micro_f1`、per-label `precision/recall/f1`
- 重点：baseline 是“纯分类”任务；VIVID 是“生成+解析后的分类评估”，两者任务形式不同，解释时需分开讲。

### 5.6 与 VIVID 的公平对比原则
- 同字段集（Top7）
- 同训练/验证子集（dense10k / val1k）
- 同 uncertain 策略口径（至少并列报告）
- 同评估脚本口径（是否允许 abstain 要明确写在表头）

## 6. 当前技术判断
- 主问题已从“塌缩”切换到“召回不足 + 字段不平衡”。
- 下一步不是盲目加步数，而是：
- 在 `GPU0` 主线下提高低召回字段的正样本可见度与梯度占比。
- 保持 `unknown/null` 在不可答时可用、在可答时受约束。
- 维持统一评估口径，避免 `abstain` 指标虚高误导结论。

## 7. 今晚到明天可执行顺序
1. 固定 `GPU0` 主线配置做一轮中等步数复跑（不直接冲 10k）。
2. 记录 per-label 召回变化，优先看 `Pleural Effusion/Edema/Pneumothorax`。
3. 并列给出 `negative` 与 `abstain` 两套评估表，但主结论只看全覆盖口径。
4. 若主线 micro/macro 继续提升，再扩展到更大样本或更多字段。

## 8. 汇报时可直接说的结论
- 我们已经证明当前方案可以脱离全空塌缩。
- 当前最可靠结果来自 `GPU0` 全覆盖口径。
- `GPU1 abstain` 是诊断增强，不是最终指标口径。
- baseline 是预训练 ViT 的纯分类上界参照，VIVID 下一步目标是稳定超过同口径 baseline，而不是只追求局部高分。

## 9. 历史结果全量补齐（V4 / V5 / V6）
### 9.1 V4（14字段，首次 answerability mask）
- 文件：`outputs/cxr_chexpert_v4/checkpoints/best.metrics.full.json`
- 结果：`json_success_rate=1.0`、`pred_nan_rate=1.0`、`macro_f1=0.0`、`micro_f1=0.2091`
- 结论：结构可生成，但语义完全塌缩到 `null`。

### 9.2 V4.2（14字段，dense10k）
- 文件：`outputs/cxr_chexpert_v4_2_dense10k/checkpoints/best.metrics.json`
- 结果：`json_success_rate=1.0`、`pred_nan_rate=0.5937`、`macro_f1=0.2553`、`micro_f1=0.5809`
- 结论：从“全空”恢复到“部分可学”，但长尾字段仍大面积失效。

### 9.3 V5 pilot（Top3, GPU1, 120步）
- 文件：`outputs/cxr_chexpert_v5_top3_gpu1_pilot_run1/checkpoints/best.metrics.json`
- 字段：`Pleural Effusion`、`Lung Opacity`、`Support Devices`
- 结果：`json_success_rate=1.0`、`pred_nan_rate=0.4880`、`macro_f1=0.5712`、`micro_f1=0.5633`
- 结论：证明“减少字段 + mask 无梯度”方向可行，但还不稳定。

### 9.4 V5.1（Top3, GPU0, 400步）
- 文件：`outputs/cxr_chexpert_v5_1_top3_gpu0_run1/checkpoints/best.metrics.json`
- 结果：`json_success_rate=1.0`、`pred_nan_rate=0.2020`、`macro_f1=0.8909`、`micro_f1=0.8518`
- 关键细节：`Pleural Effusion` 出现“高精度低召回”（`P=0.9490, R=0.5975`）。
- 结论：头部字段可以学到高质量，但字段间不平衡仍明显。

### 9.5 V6（Top7 双线并行）
- 主线（GPU0，negative）：
- 文件：`outputs/cxr_chexpert_v6_top7_gpu0_run1/checkpoints/best.metrics.json`
- 结果：`json_success_rate=1.0`、`pred_nan_rate=0.3814`、`eval_coverage=1.0`、`macro_f1=0.4087`、`micro_f1=0.5996`
- 对照（GPU1，abstain）：
- 文件：`outputs/cxr_chexpert_v6_top7_gpu1_abstain_run1/checkpoints/best.metrics.json`
- 结果：`json_success_rate=1.0`、`pred_nan_rate=0.2526`、`eval_coverage=0.4962`、`macro_f1=0.6357`、`micro_f1=0.8806`
- 结论：两条都脱离全空塌缩；但 `abstain` 因覆盖率下降，不可作为“超越”依据。

## 10. 是否超过 baseline（当前结论）
### 10.1 baseline 参照
- 参考文件：`outputs/baseline_vit_v2/best.metrics.json`
- 指标：`macro_f1=0.9125`、`micro_f1=0.8948`
- 性质：纯 ViT 分类器（直接多标签 BCE），不是生成任务。

### 10.2 当前是否超越
- **没有超越。**
- 以 `baseline_vit_v2` 的 Top7 字段估算参照（由 per-label 汇总）：`macro_f1≈0.8882`、`micro_f1≈0.9006`。
- V6 主线（Top7 全覆盖口径）显著低于 baseline 上界。
- V5.1 Top3 虽明显提升，但仍未稳定超过 baseline 的同类头部字段表现。

### 10.3 为什么没有超过（实质原因，不是一句“数据少”）
1. 监督路径效率差异：baseline 直接分类监督；VIVID 是“生成再解析”，同样样本下有效梯度更稀。
2. 字段不均衡：`blank/NaN` 背景下，部分字段可答但正样本召回难，模型趋向保守输出。
3. 目标冲突：生成任务既要结构正确又要状态正确，结构 token 与状态 token 共享学习预算。
4. 口径影响：`abstain` 会提高可评估样本上的分数，但覆盖下降后不代表真实全量能力。
5. 标签难度差异：`Pleural Effusion` 这类字段已暴露“高精度低召回”，说明主要缺陷是漏检。

## 11. 下一步怎么办（今晚到下一轮）
### 11.1 主目标
- 在 `Top7 + 全覆盖口径` 下，提高召回并维持精度，先逼近再超越 baseline。

### 11.2 立即执行顺序
1. 固定主线配置（`GPU0`，`pred_unknown_policy=negative`）继续训练，不再切换口径。
2. 对 `Pleural Effusion/Edema/Pneumothorax` 做标签级召回修复（优先提升 recall）。
3. 每轮都报告 `macro_f1/micro_f1 + per-label P/R/F1 + eval_coverage`，禁止只报单一高分。
4. `GPU1 abstain` 仅保留诊断作用，不参与“是否超过 baseline”的主判据。

### 11.3 达标判据（用于判断是否继续扩容）
- `Top7` 主线连续两轮不塌缩（`pred_nan_rate` 维持低位且稳定）。
- `Pleural Effusion` 召回显著改善，不再长期处于“高精度低召回”状态。
- `macro_f1` 和 `micro_f1` 同步提升，而不是仅靠覆盖率下降换分数。

## 12. baseline 本轮检查与结论（已完成）
### 12.1 本轮状态
- baseline 已按 `GPU0` 跑完：`outputs/baseline_vit_top7_dense10k`
- 训练终点：`Step 4500: val_loss=0.2686`
- 已补做 `best.pt` 标准评估：`outputs/baseline_vit_top7_dense10k/best.metrics.json`

### 12.2 baseline（Top7）最终指标
- `macro_f1=0.8825`
- `micro_f1=0.8956`
- 关键标签：
- `Pleural Effusion F1=0.9397`
- `Pneumothorax F1=0.5299`（baseline 里最弱项）

### 12.3 与当前 VIVID 主线差距
- VIVID（`v6_top7_gpu0_run1`）：`macro_f1=0.4087`、`micro_f1=0.5996`
- baseline（`baseline_vit_top7_dense10k`）：`macro_f1=0.8825`、`micro_f1=0.8956`
- 结论：**当前没有超过 baseline**，并且差距主要来自低召回字段（特别是 `Pleural Effusion/Edema/Pneumothorax`）。

### 12.4 下一步执行（直接可跑）
1. 继续固定主口径：`GPU0 + pred_unknown_policy=negative + eval_coverage=1.0`。
2. 针对三项低召回标签做定向修复实验，不再更换评估口径。
3. 每轮必须并列报告 `P/R/F1 + pred_nan_rate + coverage`，避免“高分但低覆盖”的假提升。
