# VIVID-Med 导师汇报

> 最后更新：2026-02-20（V12 重构版，SPD only）

## 一句话概述

用冻结 LLM 作为结构化语义监督空间训练 ViT，训练后丢弃 LLM，只交付 ViT backbone。主方法 = Frozen LLM + UMS 结构化 JSON + SPD 分解式投影 + 12-label 预训练。

---

## 1. 主方法

SPD(3组×2tokens, ortho=0.01) + 12-label + field_query_training + answerability_mask

```text
Image → ViT(trainable) → vit_features
      → SPD Projector: 多组 query tokens × cross-attention → SPD tokens → 共享MLP投影
      → Frozen LLM (Qwen2.5-1.5B) → UMS-JSON tokens
```

> HFP（多层融合）已排除：历史数据显示 SPD+HFP 反而低于 SPD 单独（LP AUC 0.7930 vs 0.8441）。
> 历史数据（V9/V10/V10.1/V11 及旧消融结果）已归档至 `profile/历史数据.md`。

---

## 2. 论文正文的各个实验

### 2.1 Table 1 — Linear Probe 主表（CheXpert 12-label，冻结 ViT）

| 行 | 实验 | 12-label F1 | 12-label AUC | 预训练口径 | 状态 |
| --- | --- | ---: | ---: | --- | --- |
| 1 | Baseline (ImageNet supervised) | 0.8845 | 0.7523 | 无需预训练 | ✅ seed=0 |
| 2 | Random mask (VRL 风格 75%) | — | — | 旧版 7-label | ⬜ 需 12-label 重跑 |
| 3 | 纯框架（= 消融链 A+UMS） | 0.9095 | 0.8461 | 12-label | ✅ seed=0 |
| 4 | **SPD（ours）**（= 消融链 A+UMS+SPD） | 0.9114 | 0.8564 | 12-label | ✅ seed=0 |

> Baseline 不依赖预训练标签数，LP 结果可直接复用。
> 纯框架与消融链 A+UMS 等价（frozen LLM + MLP projector + UMS JSON，无 SPD），直接复用 A+UMS 数据，无需单独预训练。
> Random mask 旧版是 7-label 预训练，需要新建 12-label config 重跑预训练 + LP。
> SPD 直接复用消融链 A+UMS+SPD 的 LP 结果（`ablation_A_ums_spd_12label`）。

评估协议：冻结 ViT，只训练线性分类头，3000 steps，val_loss 最低 step 报告所有指标。
每行 ×3 seeds（0/1/2），报告 mean±std。

### 2.2 Table 2 — NIH 跨域泛化（8 共同标签，25,596 样本）

| 行 | 实验 | macro_AUC | 状态 |
| --- | --- | ---: | --- |
| 1 | Baseline (ImageNet) | — | ⬜ 待跑 |
| 2 | 纯框架（= A+UMS） | — | ⬜ 待跑（LP 完成后） |
| 3 | **SPD（ours）** | — | ⬜ 待跑（LP 完成后） |
| 4 | 消融递进链各组 | — | ⬜ 待跑 |

### 2.3 Table 3 — 消融递进链（单变量递进，12-label）

已完成 12-label 预训练 + LP（seed=0），数据来自旧跑。

| 行 | 实验 | 监督格式 | SPD | macro_F1 | 12-label AUC | 状态 |
| --- | --- | --- | --- | ---: | ---: | --- |
| 1 | A（free-text, 随机句式v2） | 自由文本 | ✗ | 0.9064 | 0.8214 | ✅ seed=0 |
| 2 | A+UMS（JSON） | 结构化 JSON | ✗ | 0.9095 | 0.8461 | ✅ seed=0 |
| 3 | **A+UMS+SPD** (4 groups) | 结构化 JSON | ✓ | 0.9114 | 0.8564 | ✅ seed=0 |

论证逻辑（已验证）：

- A → A+UMS：12-label AUC +2.47pp（0.8214→0.8461）✅
- A+UMS → A+UMS+SPD：12-label AUC +1.03pp（0.8461→0.8564）✅

待补充：×3 seeds 的 mean±std，NIH 跨域评估。

扩展行（P1）：

- Q-Former proxy（num_groups=1, tokens=6, ortho=0）→ 正交分解的价值
- Frozen vs Trainable LLM → 冻结 LLM 的必要性

### 2.4 Table 4 — Full Fine-tune 补充表（解冻 ViT，12-label）

| 行 | 实验 | 12-label F1 | 12-label AUC | 状态 |
| --- | --- | ---: | ---: | --- |
| 1 | Baseline (ImageNet) | — | — | ⬜ 待跑 |
| 2 | 纯框架（= A+UMS） | — | — | ⬜ 待跑 |
| 3 | **SPD（ours）** | — | — | ⬜ 待跑 |

### 2.5 Fig.1 — Pipeline 总览

Image → ViT → SPD Projector(3 组 query tokens + cross-attention) → Frozen LLM → JSON tokens。状态：⬜ 待画。

### 2.6 Fig.2 — SPD 模块详图

3 组 query tokens 通过 cross-attention 从 ViT patch tokens 提取不同方面信息 → 正交性损失 → concat 后送入 LLM。状态：⬜ 待画。

### 2.7 Fig.3 — LP 结果柱状图

12-label F1 + AUC 柱状图。状态：⬜ 待画（部分数据已就绪）。

---

## 3. 待完成实验汇总（按优先级）

| 优先级 | 实验 | 说明 | 状态 |
| --- | --- | --- | --- |
| **P0** | Random mask 12-label 预训练 | 新建 config，重跑 | ⬜ |
| **P0** | 所有 LP ×3 seeds | mean±std | ⬜ |
| **P0** | NIH 跨域评估（所有组） | LP 完成后跑 | ⬜ |
| **P1** | Full Fine-tune 评估 | Table 4 | ⬜ |
| **P1** | Q-Former proxy 消融 | Table 3 扩展 | ⬜ |
| **P1** | Frozen vs Trainable LLM | Table 3 扩展 | ⬜ |
| **P1** | Low-data 曲线（10%/25%/50%） | Fig 或补充表 | ⬜ |
| **P2** | LLM 规模对比（0.5B/1.5B/3B） | 补充表 | ⬜ |
| **P2** | MAE baseline | Table 1 扩展 | ⬜ |

已完成：

- ✅ Baseline (ImageNet) LP（seed=0，不依赖预训练标签数）
- ✅ 消融递进链 3 组预训练 + LP（12-label，seed=0）— 其中 A+UMS = 纯框架，A+UMS+SPD = 主方法 SPD

---

## 4. 需要新建的配置文件

| 配置文件 | 用途 | 基于 |
| --- | --- | --- |
| `configs/v12_random_mask_12label.yaml` | Random mask 12-label 预训练 | 旧 random mask config + 12-label |
| 各实验对应的 LP config | LP 评估 | `linear_probe_v12_full14.yaml` 模板 |

---

## 5. 论文叙事方向

- **核心贡献**：Frozen LLM Supervision + UMS 结构化监督 + SPD 分解式 Projector
- **主表**：LP（CheXpert 12-label F1 + AUC）+ NIH 跨域泛化
- **消融**：A → A+UMS → A+UMS+SPD 单变量递进链（已完成 seed=0）
- **与 ViTP 差异**：冻结 LLM / SPD 分解 / 结构化 JSON / ViT-only 部署
- 历史 V9 GFTM/Consistency/CMSA/HFP 作为 design exploration 放 supplementary
