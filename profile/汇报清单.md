# VIVID-Med 导师汇报

> 最后更新：2026-02-16

## 一句话概述

用冻结 LLM 作为结构化语义监督空间训练 ViT，训练后丢弃 LLM，只交付 ViT backbone。在 CheXpert linear probe 和 NIH 跨域评估上均显著超越 ImageNet baseline。

---

## 1. 论文主实验（已完成）

### 1.1 主表：Linear Probe（⭐ 冻结 ViT，只训练线性分类头）

评估协议：CheXpert 14-label 全量（~29k 训练样本），冻结 ViT backbone，只训练线性分类头。Checkpoint 选择规则：取 val_loss 最低的 step 报告所有指标。指标口径：12-label（去掉 No Finding 和 Pleural Other）。

| 实验 | best step | val_loss | 12-label F1 | 12-label AUC | vs Baseline (F1 / AUC) |
| --- | ---: | ---: | ---: | ---: | --- |
| Baseline (ImageNet) | 2000 | 0.3047 | 0.8845 | 0.7523 | — |
| Random mask (VRL 风格) | 3000 | 0.2913 | 0.8603 | 0.7997 | -2.42pp / +4.74pp |
| No mask+No cons（纯框架） | 2800 | 0.2584 | 0.8939 | 0.8296 | +0.94pp / +7.73pp |
| V10 SAR+HFP | 2600 | 0.2520 | 0.8995 | 0.8392 | +1.50pp / +8.69pp |
| **V10.1 SPD** | 2600 | **0.2461** | **0.9129** | **0.8441** | **+2.84pp / +9.18pp** |

> 12-label = 去掉 No Finding（全阳性，AUC=NaN）和 Pleural Other（极低频，<50 样本，AUC 不稳定）。CheXpert 论文常见 5/10/12-label 报告口径。SPD 在 12-label F1 和 AUC 上均为最优。
>
> Random mask F1 低于 baseline（0.8603 vs 0.8845），主因 Pneumothorax F1=0.2469（SPD 为 0.6235）。VRL 风格 75% random masking 在小数据量下损害表征质量。

配置文件：
- Baseline: `configs/linear_probe_imagenet_full14.yaml`
- Random mask: `configs/linear_probe_random_mask_full14.yaml`
- No mask+No cons: `configs/linear_probe_no_mask_no_cons_full14.yaml`
- V10 SAR+HFP: `configs/linear_probe_v10_sar_hfp_full14.yaml`
- V10.1 SPD: `configs/linear_probe_v10.1_spd_full14.yaml`

Linear probe 超参数：backbone_lr=0, head_lr=1e-3, weight_decay=0, max_steps=3000, eval_interval=200, drop_path_rate=0.0, batch_size=16, grad_accum=2, bf16=true。

### 1.2 NIH 跨域泛化（8 共同标签，25,596 样本）

用 CheXpert 训练的 linear probe checkpoint 直接在 NIH ChestX-ray14 上测试，不重新训练。

| 实验 | macro_F1 | macro_AUC | vs Baseline |
| --- | ---: | ---: | --- |
| Baseline (ImageNet) | 0.2033 | 0.6159 | — |
| No mask+No cons | 0.2384 | 0.6748 | F1 +3.51pp, AUC +5.89pp |
| V10 SAR+HFP | 0.2539 | 0.6995 | F1 +5.06pp, AUC +8.36pp |
| **V10.1 SPD** | **0.2599** | **0.7121** | **F1 +5.66pp, AUC +9.62pp** |

排序与 CheXpert LP 完全一致（SPD > SAR+HFP > No mask+No cons > Baseline），表征质量提升可跨域泛化。F1 绝对值偏低是预期行为（分类头在 CheXpert 上训练，threshold=0.5 不是 NIH 最优），AUC 不受 threshold 影响，是跨域核心指标。

脚本：`python scripts/eval_nih_crossdomain.py --checkpoint <best.pt> --label <name>`

### 1.3 补充：Full Fine-tune（解冻 ViT，backbone_lr=1e-5）

| 实验 | macro_F1 | macro_AUC | val_loss |
| --- | ---: | ---: | ---: |
| Baseline (ImageNet) | 0.8987 | 0.7927 | 0.2521 |
| SVRL V9 (7-label) | 0.9064 | 0.8210 | 0.2455 |
| V10 SAR+HFP | 0.9168 | 0.8367 | 0.2454 |
| V10.1 SPD | 0.9198 | 0.8551 | 0.2316 |
| No mask+No cons | 0.9152 | 0.8563 | 0.2465 |

Full fine-tune 下 baseline 已经 F1=0.8987，提升空间被压缩。Linear probe 能更好放大表征质量差异（AUC 差距从 +4-6pp 放大到 +11-14pp），因此以 LP 为主表。

---

## 2. 消融实验

### 2.1 论文消融矩阵（以最终方法 V11 为中心）

以 V11（SPD + Label-Balanced Loss）为 full method，逐一去掉组件验证贡献。评估方式：Linear Probe（12-label 口径，冻结 ViT）。

| # | 实验 | SPD | LBL | 说明 | 12-label F1 | 12-label AUC | 状态 |
| --- | --- | --- | --- | --- | ---: | ---: | --- |
| A | **V11 full (SPD+LBL)** | ✓ | ✓ | 最终方法 | — | — | ⬜ 待跑 |
| B | -LBL (=V10.1 SPD) | ✓ | ✗ | 去掉 label balance | 0.9129 | 0.8441 | ✅ 已有 |
| C | -SPD (=框架+LBL) | ✗ | ✓ | 去掉 SPD | — | — | ⬜ 待跑 |
| D | -SPD-LBL (=纯框架) | ✗ | ✗ | 基础框架 | 0.8939 | 0.8296 | ✅ 已有 |
| E | 替换: SAR+HFP | ✗(SAR+HFP) | ✗ | 替代 projector 设计 | 0.8995 | 0.8392 | ✅ 已有 |
| F | 替换: Random mask (VRL) | ✗(random 75%) | ✗ | ViTP 风格 | 0.8603 | 0.7997 | ✅ 已有 |
| G | ImageNet baseline | — | — | 无预训练 | 0.8845 | 0.7523 | ✅ 已有 |

预期论证逻辑：
- A > B → LBL 有正面贡献（改善低频标签 AUC）
- A > C → SPD 有正面贡献（结构化分解提升 F1）
- A > D → SPD+LBL 组合优于纯框架
- A > E → SPD+LBL 优于 SAR+HFP 替代设计
- A > F → 优于 ViTP 风格 random masking
- D > G → 框架本身（frozen LLM supervision）有显著贡献

配置文件：
- V11 full: `configs/v11_spd_label_balance.yaml` → LP: `configs/linear_probe_v11_spd_lbl_full14.yaml`
- -LBL: `configs/v10.1_spd.yaml` → LP: `configs/linear_probe_v10.1_spd_full14.yaml`
- -SPD: `configs/ablation_v11_lbl_only.yaml` → LP: `configs/linear_probe_lbl_only_full14.yaml`
- -SPD-LBL: `configs/ablation_v9_no_mask_no_consistency.yaml` → LP: `configs/linear_probe_no_mask_no_cons_full14.yaml`
- SAR+HFP: `configs/v10_sar_hfp.yaml` → LP: `configs/linear_probe_v10_sar_hfp_full14.yaml`
- Random mask: `configs/ablation_v9_random_mask.yaml` → LP: `configs/linear_probe_random_mask_full14.yaml`

### 2.2 V9 设计探索（Supplementary / Appendix）

V9 SVRL 原始消融（GFTM + Consistency + CT 混合），结论：三个组件均无正面贡献，核心价值来自 frozen LLM supervision 框架本身。此结论指导了 V10/V10.1/V11 的设计方向。

| 实验 | GFTM | Cons | CT | macro_F1 (FT) | macro_AUC (FT) |
| --- | --- | --- | --- | ---: | ---: |
| SVRL-full | ✓ | ✓ | ✓ | 0.9064 | 0.8210 |
| -GFTM | ✗ | ✓ | ✓ | 0.9208 | 0.8423 |
| -Consistency | ✓ | ✗ | ✓ | 0.9130 | 0.8439 |
| No mask+No cons | ✗ | ✗ | ✗ | 0.9152 | 0.8563 |
| -CT (CXR only) | ✓ | ✓ | ✗ | 0.9149 | 0.8446 |

关键发现：GFTM hard masking 导致预训练过拟合（best@500-1000 后退化），CT 混合拖累 CXR transfer。这些 negative findings 促使我们转向轻量级增强（SPD + LBL）。

---

## 3. V10/V10.1 新模块（基于消融结论设计）

消融结论：纯框架最优，GFTM hard masking 有害。V10/V10.1 在纯框架基础上加轻量级增强模块。

### 3.1 V10 SAR+HFP

- **SAR (Soft Attention Reweighting)**：用 CLS-to-patch attention 倒数作为软权重，放大低注意力 tokens，替代 GFTM 的 hard masking。可学习参数 α 控制加权强度。
- **HFP (Hierarchical Feature Projection)**：提取 ViT 第 4/8/12 层特征，每层一个 MLP projector → concat → fusion，让梯度传到 ViT 所有层。
- 预训练配置：`configs/v10_sar_hfp.yaml`（10k steps, CXR+CT）
- Linear probe 结果：12-label F1=0.8995, AUC=0.8392（vs Baseline +1.50pp/+8.69pp）

### 3.2 V10.1 SPD（当前最优）

- **SPD (Structured Prediction Decomposition)**：3 组可学习 query tokens（每组 2 个），通过 cross-attention 从 ViT 特征中自适应提取不同方面信息。正交性损失（ortho_weight=0.01）鼓励各组 attention pattern 互不重叠。
- 与 HFP 的区别：HFP 纵向多尺度（多层融合）→ SPD 横向多方面（单层分解）
- 预训练配置：`configs/v10.1_spd.yaml`（10k steps, CXR+CT）
- Linear probe 结果：**12-label F1=0.9129**（所有实验最高），**AUC=0.8441**（所有实验最高）
- NIH 跨域：**F1=0.2599, AUC=0.7121**（所有实验最高）

---

## 4. 分割 Transfer（补充实验，已完成）

KiTS21 2.5D，ViT backbone + ViTSegHead（4×ConvTranspose2d），Dice+CE Loss，15000 steps。

| 实验 | mean_dice_fg | dice_kidney | dice_tumor | dice_cyst |
| --- | ---: | ---: | ---: | ---: |
| Baseline (ImageNet) | 0.3452 | 0.5986 | 0.3822 | 0.0549 |
| V9 SVRL | 0.3388 | 0.6037 | 0.3726 | 0.0401 |

分割上基本持平，定位为补充实验。分类 transfer 为主表。

---

## 5. 待完成实验

| # | 实验 | 目的 | 优先级 |
| --- | --- | --- | --- |
| 1 | V11 pretrain (SPD+LBL) | 最终方法预训练 | P0 |
| 2 | V11 LP + NIH eval | 验证 V11 主指标 | P0 |
| 3 | LBL-only 消融 pretrain + LP | 验证 SPD 贡献 | P0 |
| 4 | 多 seed（×3 seeds: 0,1,2） | mean±std + paired t-test，统计显著性 | P1 |
| 5 | Low-data 曲线（10%/25%/50%） | 数据效率验证 | P1 |
| 6 | MAE/MedMAE baseline | 自监督 vs LLM 监督 | P2 |

---

## 6. 论文叙事方向

- **核心贡献**：Frozen LLM Supervision for Visual Representation Learning — 用冻结 LLM 作为结构化语义监督空间，训练后丢弃 LLM，只交付 ViT backbone
- **方法组件**：(1) Frozen LLM supervision 框架，(2) SPD 结构化预测分解，(3) Label-Balanced Loss 低频标签加权
- **主表**：Linear Probe（CheXpert 12-label F1 + 12-label AUC）+ NIH 跨域泛化
- **消融**：以 V11 为中心，逐一去掉 SPD / LBL 验证贡献；替换实验（SAR+HFP, Random mask）验证设计选择
- **与 ViTP 的差异**：冻结 LLM（vs 训练 LLM）、SPD 分解（vs random token drop）、结构化 JSON 预测（vs caption generation）、label-balanced loss（vs uniform loss）
- V9 GFTM/Consistency/CMSA 作为 design exploration 放 supplementary
