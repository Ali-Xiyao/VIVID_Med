# VIVID-Med：写作参考

> 最后更新：2026-02-21（V12 重构版，SPD only，全部实验已完成）

## 1. 论文结构（MICCAI 8 页）

1. **Introduction**：one-hot 标签局限 → 用冻结 LLM 的语义空间训练 ViT → 训练后丢弃 LLM
2. **Related Work**：ViT 预训练（MAE/DINO）、医学 VLP/VLM、ViTP（重点对比）
3. **Method**：Frozen LLM Supervision 框架 + UMS 结构化监督 + SPD
4. **Experimental Setup**：数据集、评估协议（linear probe + cross-domain）
5. **Results**：主表（LP 12-label F1 + AUC）+ 跨域泛化（NIH）
6. **Ablations**：A → A+UMS → A+UMS+SPD 单变量递进链 + Q-Former proxy
7. **Limitations & Ethics**

## 2. Introduction 三句话模板

1. 现有医学视觉训练多依赖 one-hot/弱标签监督，难以编码概念相似性与共现结构。
2. 我们提出将冻结 LLM 视为语义监督空间，通过结构化 JSON token 预测训练 ViT 表征，训练后丢弃 LLM 只保留 ViT。
3. SPD（Structured Prediction Decomposition）通过多组正交 query tokens 分解视觉表征，在 LP 和跨域评估下验证表征质量。

## 3. Contribution Bullets（英文，放 Intro 末尾）

- **Frozen LLM Supervision Framework.** We propose using a frozen LLM as a structured semantic supervision space for training visual representations. The LLM is discarded after training, yielding a deployable ViT-only backbone.
- **Unified Medical Schema (UMS).** We design a structured JSON supervision format that encodes findings as verifiable field-state pairs, enabling richer gradient signals compared to free-text supervision.
- **Structured Prediction Decomposition (SPD).** We introduce learnable query token groups with cross-attention and orthogonality regularization, enabling the projector to decompose structured predictions into complementary aspects and provide richer gradient signals to the ViT encoder.
- **Comprehensive Evaluation.** We validate representation quality through linear probing on CheXpert (12-label), cross-domain transfer to NIH ChestX-ray14, and a single-variable ablation chain (free-text → UMS → UMS+SPD) that isolates each component's contribution.

## 4. Related Work 定位段（英文 paper-ready）

### Visual instruction pretraining

Recent works use language supervision to improve visual representations. A representative line embeds a vision encoder into a VLM and optimizes next-token prediction on instruction data, then extracts the pretrained backbone for downstream tasks. While effective, these approaches rely on free-form textual responses and often target improving the overall VLM.

### Medical foundation models

Medical foundation models are commonly learned via self-supervised objectives (MAE, DINO) or contrastive learning (BiomedCLIP). However, clinical deployment prioritizes robustness under domain shift, calibration, and avoidance of clinically critical errors, which are not explicitly controlled by most generic pretraining objectives.

### Our positioning

We differ in both goal and supervision design. Our goal is a deployable ViT-only medical backbone, not an instruction-following VLM. We treat a frozen LLM as a semantic teacher and update only ViT+projector via structured, verifiable token supervision. SPD decomposes visual features into orthogonal aspects for richer supervision signals.

> **One-sentence contrast to ViTP:** Unlike visual instruction pretraining that learns from free-form responses with a trainable LLM, we perform verifiable semantic distillation where a frozen LLM provides structured supervision to train a deployable ViT-only model.

## 5. Figure 清单（建议 3-4 张）

1. **Fig.1 Pipeline 总览**：Image → ViT → SPD Projector (3 组 query tokens + cross-attention) → Frozen LLM → JSON tokens，强调 LLM 冻结、梯度只更新 ViT+Projector、正交性损失
2. **Fig.2 SPD 模块详图**：3 组 query tokens 通过 cross-attention 从 ViT patch tokens 提取不同方面信息 → 正交性损失鼓励各组关注不同区域 → 与 ViT tokens concat 后送入 LLM
3. **Fig.3 评估结果**：LP 对比柱状图（12-label F1 + AUC）
4. **Fig.4 消融对比**：递进链柱状图 / SPD attention pattern 多样性可视化

## 6. Table 清单（建议 3-4 张）

1. **Table 1 LP 主表**：Baseline(ImageNet) / MAE / BiomedCLIP / Random mask / 纯框架（=A+UMS） / SPD（12-label F1 + AUC，mean±std）
2. **Table 2 NIH 跨域泛化**：Baseline / MAE / BiomedCLIP / 纯框架 / SPD，8-label AUC，证明表征可迁移
3. **Table 3 消融递进链**：A → A+UMS → A+UMS+SPD，单变量递进 + Q-Former proxy 扩展行

## 7. 对比实验策略

### 7.1 必做（P0）— 已全部完成 ✅

| 对比对象 | 证明什么 | 放在哪 |
| --- | --- | --- |
| ImageNet supervised ViT | SPD > 通用监督预训练 | Table 1 + Table 2 |
| MAE (ImageNet self-supervised) | SPD > 自监督预训练 | Table 1 + Table 2 |
| BiomedCLIP (Medical VL) | SPD > 医学领域 contrastive（15M vs 30k） | Table 1 + Table 2 |
| 纯框架（= 消融链 A+UMS） | SPD 在框架基础上有增益 | Table 1 + Table 3 |
| Random mask（VRL 风格 75%） | SPD > ViTP 风格 random masking | Table 1 |
| NIH 跨域泛化 | 表征质量可跨域迁移 | Table 2 |
| 消融递进链 | UMS / SPD 各自独立贡献 | Table 3 |

### 7.2 强烈建议（P1）— 已全部完成 ✅

| 对比对象 | 证明什么 |
| --- | --- |
| 多 seed 重复 (3 seeds) | 统计显著性 |
| Q-Former proxy（单组 SPD） | 正交分解 vs 普通 cross-attention |

### 7.3 与 ViTP 的差异化叙事

| 维度 | ViTP | 我们 |
| --- | --- | --- |
| LLM 角色 | 训练 LLM（目标空间漂移） | 冻结 LLM（稳定语义锚点） |
| Token 处理 | 随机 drop 75% | SPD cross-attention 分解（不丢弃） |
| Projector | 简单 MLP | SPD 多组 query + 正交性约束 |
| 输出格式 | 自然语言 | 可验证 UMS-JSON |
| 部署 | 需要 LLM | ViT-only |

### 7.4 写作注意事项

- ViTP 无法直接对比（无开源权重），用 random mask 消融作为 proxy
- 不要在 Related Work 中过度攻击 ViTP，定位为"同属大类，不同分叉"
- 强调冻结 LLM 是优势（稳定锚点）而非限制
- 主表口径用 LP（表征质量差异最大化）
- 指标口径：12-label F1 + 12-label AUC

## 8. 常见问题速查（QA）

**Q: 最终交付物是什么？**
A: 医学 ViT backbone checkpoint。部署时不需要 LLM，直接接下游 head。

**Q: 为什么用 LP 而不是 FT？**
A: LP 是表征学习标准评估（SimCLR/DINO/MAE/ViTP 均采用），FT 压缩差异。

**Q: 为什么报 12-label？**
A: No Finding（全阳性，AUC=NaN）和 Pleural Other（<50 样本）导致 macro 指标不稳定。12-label 口径在 CheXpert 论文中常见。

**Q: SPD 的 novelty 在哪？**
A: 现有 VLP projector 多为简单 MLP 或 Q-Former（面向 VLM 推理），SPD 专为表征学习设计：多组 query tokens 通过正交性约束分解不同语义方面，为 ViT 提供更丰富的梯度信号。

**Q: 为什么不用 HFP？**
A: 历史实验表明 SPD+HFP 联合反而低于 SPD 单独（LP AUC 0.7930 vs 0.8441）。SPD 的 cross-attention 已能提供足够的梯度信号，HFP 叠加增加复杂度但干扰训练。

**Q: 和 ViTP 是不是一类？**
A: 同属"用语言信号塑造视觉骨干"大类，但关键分叉：(1) 冻结 vs 训练 LLM，(2) SPD 结构化分解 vs 随机 mask，(3) 可验证 JSON vs 自然语言。

---

## 9. UMS JSON 真实示例

以下为 CheXpert 数据集中的真实 UMS 样本（从 `chexpert_ums_val.jsonl` 提取），展示不同临床场景下的结构化表示。

### 示例 1：多阳性 + uncertain（Female, 41y, AP view）

```json
{
  "modality": "CXR",
  "anatomy": ["chest"],
  "study_view": "AP",
  "findings": {
    "Lung Opacity": {"state": "present"},
    "Pneumonia": {"state": "uncertain"},
    "Pleural Effusion": {"state": "present"},
    "Cardiomegaly": {"state": null},
    "Atelectasis": {"state": null},
    "...": "其余 finding state=null"
  },
  "answerability": {
    "Lung Opacity": true,
    "Pneumonia": true,
    "Pleural Effusion": true,
    "Cardiomegaly": false,
    "...": "state=null 的 finding answerability=false"
  },
  "uncertainty": {
    "Lung Opacity": false,
    "Pneumonia": true,
    "Pleural Effusion": false
  }
}
```

要点：Lung Opacity 和 Pleural Effusion 确定阳性，Pneumonia 标记为 uncertain（CheXpert U-label），其余 finding 无标注（state=null, answerability=false）。

### 示例 2：阳性 + 阴性混合（Male, 68y, PA view）

```json
{
  "modality": "CXR",
  "study_view": "PA",
  "findings": {
    "Cardiomegaly": {"state": "absent"},
    "Lung Opacity": {"state": "present"},
    "Pneumonia": {"state": "present"},
    "Pleural Effusion": {"state": "absent"}
  },
  "answerability": {
    "Cardiomegaly": true,
    "Lung Opacity": true,
    "Pneumonia": true,
    "Pleural Effusion": true
  },
  "uncertainty": {
    "Cardiomegaly": false,
    "Lung Opacity": false,
    "Pneumonia": false,
    "Pleural Effusion": false
  }
}
```

要点：4 个 finding 均有确定标注（answerability=true, uncertainty=false），包含 present 和 absent 两种状态。

### 示例 3：多 uncertain（Female, 82y, LAT view）

```json
{
  "modality": "CXR",
  "study_view": "LAT",
  "findings": {
    "Lung Opacity": {"state": "present"},
    "Consolidation": {"state": "uncertain"},
    "Atelectasis": {"state": "uncertain"},
    "Pleural Effusion": {"state": "uncertain"}
  },
  "answerability": {
    "Lung Opacity": true,
    "Consolidation": true,
    "Atelectasis": true,
    "Pleural Effusion": true
  },
  "uncertainty": {
    "Lung Opacity": false,
    "Consolidation": true,
    "Atelectasis": true,
    "Pleural Effusion": true
  }
}
```

要点：侧位片（LAT），3 个 finding 为 uncertain，体现 CheXpert 数据集中 U-label 的高频特性。

### 示例 4：多阳性 + uncertain 混合（Female, 90y, AP view）

```json
{
  "modality": "CXR",
  "study_view": "AP",
  "findings": {
    "Enlarged Cardiomediastinum": {"state": "uncertain"},
    "Cardiomegaly": {"state": "present"},
    "Lung Opacity": {"state": "absent"},
    "Edema": {"state": "absent"},
    "Atelectasis": {"state": "present"},
    "Pleural Effusion": {"state": "absent"}
  },
  "answerability": {
    "Enlarged Cardiomediastinum": true,
    "Cardiomegaly": true,
    "Lung Opacity": true,
    "Edema": true,
    "Atelectasis": true,
    "Pleural Effusion": true
  },
  "uncertainty": {
    "Enlarged Cardiomediastinum": true,
    "Cardiomegaly": false,
    "Lung Opacity": false,
    "Edema": false,
    "Atelectasis": false,
    "Pleural Effusion": false
  }
}
```

要点：6 个 finding 有标注（answerability 最多的样本类型），present/absent/uncertain 三种状态共存。

### UMS 设计要点（论文 Method 用）

- **state**: `"present"` / `"absent"` / `"uncertain"` / `null`（无标注）
- **answerability**: 标记该 finding 是否有标注（`true` = 有标注，`false` = 无标注/blank）
- **uncertainty**: 标记是否为 CheXpert U-label（`true` = uncertain）
- 训练时 `answerability=false` 的 finding 不参与 loss 计算（`answerability_mask: false_weight=0.0`）
- LLM 需要预测完整 JSON 结构，包括 null 状态，这迫使模型学习"不确定"的语义表示
- 与 one-hot 标签的关键区别：UMS 编码了 finding 间的共现结构（同一 JSON 中多个 field-state 对）和不确定性信息
