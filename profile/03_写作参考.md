# VIVID-Med：写作参考

> 最后更新：2026-02-15

## 1. 论文结构（MICCAI 8 页）

1. **Introduction**：one-hot 标签局限 → 用冻结 LLM 的语义空间训练 ViT → 训练后丢弃 LLM
2. **Related Work**：ViT 预训练（MAE/DINO）、医学 VLP/VLM、ViTP（重点对比）
3. **Method**：Frozen LLM Supervision 框架 + SPD/SAR+HFP 模块
4. **Experimental Setup**：数据集、评估协议（linear probe + low-data + cross-domain + full fine-tune）
5. **Results**：主表（linear probe）+ 数据效率曲线 + 跨域泛化
6. **Ablations**：消融矩阵（证明框架本身是核心，组件叠加反而有害）
7. **Limitations & Ethics**

## 2. Introduction 三句话模板

1. 现有医学视觉训练多依赖 one-hot/弱标签监督，难以编码概念相似性与共现结构，在跨域与干扰下容易出现 shortcut。
2. 我们提出将冻结 LLM 视为语义监督空间，通过结构化 JSON token 预测训练 ViT 表征，训练后丢弃 LLM 只保留 ViT。
3. 在 linear probe、low-data、cross-domain 等标准评估下验证表征质量。

> 类比：**老师（LLM）只在训练时教，毕业后上岗的是学生（ViT）**。

## 3. Contribution Bullets（英文，放 Intro 末尾）

- **Frozen LLM Supervision Framework.** We propose using a frozen LLM as a structured semantic supervision space for training visual representations. The LLM is discarded after training, yielding a deployable ViT-only backbone.
- **SPD / SAR+HFP.** We introduce lightweight modules (Structured Prediction Decomposition / Soft Attention Reweighting + Hierarchical Feature Projection) that enhance the framework without the pitfalls of hard token masking.
- **Comprehensive Evaluation.** We validate representation quality through linear probing, low-data fine-tuning, cross-domain transfer, and systematic ablations, following the standard protocol of representation learning (SimCLR/DINO/MAE).

## 4. Related Work 定位段（英文 paper-ready）

### Visual instruction pretraining

Recent works use language supervision to improve visual representations. A representative
line embeds a vision encoder into a VLM and optimizes next-token prediction on instruction
data, then extracts the pretrained backbone for downstream tasks. While effective, these
approaches rely on free-form textual responses and often target improving the overall VLM.

### Medical foundation models

Medical foundation models are commonly learned via self-supervised objectives (MAE, DINO)
or contrastive learning (BiomedCLIP). However, clinical deployment prioritizes robustness
under domain shift, calibration, and avoidance of clinically critical errors, which are not
explicitly controlled by most generic pretraining objectives.

### Our positioning

We differ in both goal and supervision design. Our goal is a deployable ViT-only medical
backbone, not an instruction-following VLM. We treat a frozen LLM as a semantic teacher
and update only ViT+projector via structured, verifiable token supervision.

> **One-sentence contrast to ViTP:** Unlike visual instruction pretraining that learns from
> free-form responses with a trainable LLM, we perform verifiable semantic distillation
> where a frozen LLM provides structured supervision to train a deployable ViT-only model.

## 5. Figure 清单（建议 3-4 张）

1. **Fig.1 Pipeline 总览**：Image → ViT → Projector → Frozen LLM → JSON tokens，强调 LLM 冻结、梯度只更新 ViT+Projector
2. **Fig.2 模块对比**：SAR 软加权 vs GFTM hard masking / SPD cross-attention query 示意
3. **Fig.3 评估结果**：linear probe 对比 + low-data 曲线
4. **Fig.4 消融热力图**：组件叠加 vs 纯框架的性能对比

## 6. Table 清单（建议 3-4 张）

1. **Table 1 Linear Probe 主表**：ImageNet baseline vs Best pretrain（冻结 ViT，只训练线性头）
2. **Table 2 Full Fine-tune + Cross-domain**：CheXpert 14-label + NIH external test
3. **Table 3 消融矩阵**：GFTM/Consistency/CT 各组件的开关组合
4. **Table 4 Low-data 曲线**：10%/25%/50%/100% 训练数据下的性能

## 7. 对比实验策略

### 7.1 必做（P0）

| 对比对象 | 证明什么 | 放在哪 |
| --- | --- | --- |
| ImageNet supervised ViT | 框架 > 通用监督预训练 | Table 1 主表 |
| 自身消融（6 个配置） | 框架本身是核心，组件叠加有害 | Table 3 消融 |
| Linear probe | 表征质量直接对比（不受 fine-tune 稀释） | Table 1 主表 |

### 7.2 强烈建议（P1）

| 对比对象 | 证明什么 | 放在哪 |
| --- | --- | --- |
| Low-data 曲线 (10%/25%/50%) | 表征质量在数据稀缺时优势更大 | Table 4 |
| NIH cross-domain | 跨机构泛化能力 | Table 2 |
| 多 seed 重复 (3 seeds) | 统计显著性 | mean±std |

### 7.3 加分项（P2）

| 对比对象 | 证明什么 |
| --- | --- |
| MAE/MedMAE | 框架（LLM 监督）> 自监督重建 |
| Random mask 消融 | SAR 软加权 > VRL 随机 mask |

### 7.4 与 ViTP 的差异化叙事

| 维度 | ViTP | 我们 |
| --- | --- | --- |
| LLM 角色 | 训练 LLM（目标空间漂移） | 冻结 LLM（稳定语义锚点） |
| Token 处理 | 随机 drop 75%，固定比例 | SAR 软加权（不丢弃 token） |
| 输出格式 | 自然语言 | 可验证 UMS-JSON |
| 评估维度 | 16 benchmarks（CVPR 篇幅） | linear probe + low-data + cross-domain（MICCAI 篇幅） |
| 医学评估 | 只有分割（AMOS/BraTS） | 分类 + 分割 + 跨域 + 低数据 |

### 7.5 写作注意事项

- ViTP 无法直接对比（无开源权重），用 random mask 消融作为 proxy
- 不要在 Related Work 中过度攻击 ViTP，定位为"同属大类，不同分叉"
- 强调冻结 LLM 是优势（稳定锚点）而非限制
- 主表口径用 linear probe（表征质量差异最大化）

## 8. 常见问题速查（QA）

**Q: 最终交付物是什么？**
A: 医学 ViT backbone checkpoint。部署时不需要 LLM，直接接下游 head。

**Q: ViT-only transfer 为什么关键？**
A: 直接回答"提升来自 LLM 兜底还是 ViT 表征变强"。

**Q: 和 ViTP 是不是一类？**
A: 同属"用语言信号塑造视觉骨干"大类，但关键分叉：(1) 冻结 vs 训练 LLM，(2) SAR 软加权 vs 随机 mask，(3) 可验证 JSON vs 自然语言。

**Q: 消融结果显示组件都没用，novelty 在哪？**
A: 核心 novelty 是 Frozen LLM Supervision 框架本身（AUC +6.4pp vs baseline）。消融结果本身有价值：证明简单框架优于复杂组合，为后续研究提供指导。V10/V10.1 的 SAR+HFP/SPD 是在纯框架基础上的增强。

**Q: 为什么用 linear probe 而不是 full fine-tune？**
A: Full fine-tune 下 baseline F1=0.90 已经很高，预训练方法的提升空间被压缩。Linear probe 是表征学习领域标准评估（SimCLR/DINO/MAE/ViTP 均采用），冻结 backbone 直接衡量表征质量。

**Q: 这是不是多任务学习？**
A: 不是。我们把不同监督统一到 UMS-JSON 的结构化 token 预测接口（单一输出空间），而非多个任务头。

## 9. 当前最新结果速查（2026-02-15）

### Full Fine-tune（14-label 全量口径）

| 实验 | macro_F1 | macro_AUC |
| --- | ---: | ---: |
| Baseline (ImageNet) | 0.8987 | 0.7927 |
| SVRL (14-label pretrain) | 0.9144 | 0.8261 |
| -GFTM 消融 | **0.9208** | 0.8423 |
| No mask+No consistency | 0.9152 | **0.8563** |

### 关键结论

- 纯框架（No mask+No consistency）AUC=0.8563，比 baseline +6.4pp
- 所有消融均优于 SVRL-full，核心价值来自框架本身
- Linear probe 评估待跑，预期差距更大（5-10pp）
