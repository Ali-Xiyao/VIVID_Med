# 10 训练结果记录（更新至 2026-02-09）

## 1. 目标与当前状态
- 目标：用冻结 LLM 的结构化监督信号训练 ViT/Projector，输出可验证 JSON。
- 当前状态：核心架构未变，JSON 解析稳定性已修复，但分类性能仍显著低于 ViT baseline。

## 2. 关键实验结果（已完成）
| 实验 | 数据划分 | 样本数 | json_success_rate | pred_nan_rate | macro_f1 | micro_f1 |
|---|---|---:|---:|---:|---:|---:|
| VIVID（早期 full eval）`outputs/cxr_chexpert/checkpoints/best.metrics.full.json` | train/val 固定划分 | 1000 | 1.0 | 0.6137 | 0.3702 | - |
| VIVID v2（塌缩版本）`outputs/cxr_chexpert_v2/checkpoints/best.metrics.full.v2.json` | train/val 固定划分 | 1000 | 1.0 | 0.0000 | 0.0175 | 0.2186 |
| VIVID quick rerun（修复后）`outputs/cxr_chexpert_quick_rerun/checkpoints/best.metrics.quick.json` | quick val | 200 | 1.0 | 0.8571 | 0.1286 | 0.4313 |
| VIVID v4（answerability mask, `false_weight=0.0`）`outputs/cxr_chexpert_v4/checkpoints/best.metrics.full.json` | train(29k)/val(1k) | 1000 | 1.0 | 1.0000 | 0.0000 | 0.2091 |
| ViT baseline v2 `outputs/baseline_vit_v2/best.metrics.json` | 同划分对比 | 1000 | - | - | 0.9125 | 0.8948 |

说明：
- `json_success_rate=1.0` 说明结构化输出流程已通。
- 但 `pred_nan_rate` 高、`macro_f1` 低，说明模型在状态判别上仍偏“空/弱预测”。
- V4 已完成训练与 full eval，但出现 `pred_nan_rate=1.0`（预测几乎全 `null`），当前版本未达到可用状态。

## 3. 问题定位（当前结论）
1. **状态词监督不平衡**：训练目标里 `null` 比例高，导致模型倾向保守输出。  
2. **LM loss 稀释**：固定 JSON 模板 token 太多，关键状态 token（present/absent/uncertain/null）梯度占比不足。  
3. **现象一致**：解析成功不等于判别有效，当前瓶颈是“可解析”到“可判别”的迁移。

### 3.1 CheXpert findings 的 blank/NaN ≈ 70%（不是读取 bug）
- **结论**：当前大量 `null` 来自 CheXpert 原始标注的 `blank`（在 CSV 里表现为 `NaN`），语义是“报告未提及该 observation”，不是我们没读到标注。
- **代码口径（显式设计）**
- `data/dataset/prepare_chexpert.py:70`：`NaN -> state=None`（写入 JSON 即 `null`）。
- `data/dataset/prepare_chexpert.py:143`：`answerability[label] = not isna(raw)`，所以 `blank/NaN` 会变成 `answerability=false`。
- `data/chexpert_dataset.py`：训练标签向量里 `state=null -> label=NaN`（评估时对 `y_true` 的 NaN 位置做 mask）。
- 验证：在 `data/dataset/processed/chexpert_ums.jsonl` 中，`state=null` 的 finding 其 `answerability=false` 对齐率为 100%（说明这是数据语义，不是解析/读取错误）。
- **统计证据（30k 采样）**
- `data/dataset/processed/chexpert_sampled_30k.csv`：14 个 findings 总体 `NaN(blank)` 占比约 **70.47%**（与 UMS JSONL 中 `state=null` 占比一致）。
- Top missing（30k）：`Pleural Other 92.45%`、`No Finding 91.50%`、`Fracture 89.14%`、`Lung Lesion 88.82%`、`Pneumonia 83.63%`。
- 完整逐标签表见：`profle/schema/coverage_table.md:25`（present/absent/uncertain/missing/覆盖率）。
- **关键影响**：`configs/cxr_chexpert.yaml` 的 prompt 强制 “findings 包含全部 14 条”，但真实监督里大部分是 `blank->null`，导致 token 监督极端稀释，模型倾向学到“安全输出 null”，从而出现 `json_success_rate` 高但 `macro_f1` 低的现象。

### 3.2 缺失率怎么处理（从不改 UMS 到伪标签/字段重构）
- **对比口径（必须写清楚）**
- 严格口径（UMS 语义）：`blank/NaN -> unknown`，在 `y_true` 中为 NaN 并在评估时 mask 掉（当前实现）。
- baseline 口径（公平对比）：额外报告一套把 `blank->absent` 的指标（仅用于对比，不等价于 UMS 真值）。
- **路线 A：训练目标层面（不改变 UMS 语义）**
- 已落地：V3 `state-aware token weighting`（抑制 `null` token 主导）。
- 下一步（推荐）：`answerability-aware loss`，对 `answerable=false` 的 finding block 整段降权/屏蔽，而不是只给 `null` 字符串降权。
- 备选：增加轻量多标签分类辅助头（只更新 ViT/Projector），给 `present/absent/uncertain` 提供更密的梯度，与 JSON 生成联合训练。
- **路线 B：字段/输出设计回顾（减少“必须显式输出 14 个 null”的结构性问题）**
- 备选 schema：把 findings 从“全量字典 + 大量 null”改成“稀疏列表/集合表示”，例如 `{"present":[...],"uncertain":[...],"absent":[...]}`；未出现即隐式 unknown。
- 好处：显著减少模板 token 与 `null` token；更贴合稀疏监督。
- 风险：需要同步调整 verifier/eval 口径（可通过后处理还原为 14 label JSON 保持兼容）。
- **路线 C：数据层面稠密化（teacher 伪标签 + 小规模人工标注）**
- 伪标签：用 teacher（例如 ViT baseline，按常见 CheXpert 口径 `blank->absent`）对 `blank` 位置做高置信度填充（只填最确定的 `absent/present`，其余仍保留 null）。
- 人工标注（Codex 做工具）：构建一个 calibration set（建议 200–500 张，重点覆盖高 missing 标签），用于校准伪标签阈值并验证“blank 更像 absent 还是 unknown”。
- 标注产物：`annotation_queue.csv`（待标注清单）+ `manual_labels.csv`（人工结果，写入 `.gitignore`，不入库）。
- 采样建议：对 `Pleural Other/Fracture/Lung Lesion/Pneumonia` 各抽 50–100 张；其中 20% 双人复标用于一致性评估。
- 标注字段：`sample_id/original_path/label_name/state(present|absent|uncertain|skip)/annotator/timestamp/notes`。

## 4. 已完成的 V3 代码改动（未启动训练）
本次只改代码，不启动训练。

### 4.1 状态词加权损失（核心）
- 文件：`training/losses.py`
- 改动：`StructuredLoss` 增加 state-aware token weighting 能力：
  - 支持 `token_id_weights`
  - 支持 `token_sequence_weights`
  - 支持 `default_token_weight`
- 作用：放大 `present/uncertain/absent` 的学习信号，抑制 `null` 过度主导。

### 4.2 训练器接入 V3 损失
- 文件：`training/trainer.py`
- 改动：
  - 新增 `token_weighting` 配置入口
  - 自动从 tokenizer 构建状态词权重（含多种 JSON 变体）
  - 训练/验证统一使用加权 token loss（若启用）
  - `autocast` 改为新接口优先，兼容旧接口
- 作用：无需改模型主结构即可切换到 V3 训练策略。

### 4.3 模型输出补充 labels（供加权损失使用）
- 文件：`models/vivid_model.py`
- 改动：`forward(return_dict=True)` 增加 `"labels"` 返回字段。

### 4.4 训练脚本接入
- 文件：`scripts/train_cxr.py`
- 改动：
  - 向 `VIVIDTrainer` 传入 `token_weighting`
  - debug 模式自动禁用 token weighting（保证调试轻量）

### 4.5 新增 V3 配置
- 文件：`configs/cxr_chexpert_v3.yaml`
- 关键参数：
  - `token_weighting.enabled: true`
  - `present: 3.0`
  - `uncertain: 2.5`
  - `absent: 1.5`
  - `null: 0.35`
- 训练输出目录：`outputs/cxr_chexpert_v3`

### 4.6 语法检查
- 已执行（未训练）：`py_compile` 检查通过。

## 5. 下一步执行建议（基于 V4 实测）
1. 不建议继续用 `false_weight=0.0` 直接全量跑；先做 `V4.1` 小步试验（1000-2000 steps）。  
2. 优先扫描 `false_weight`：`0.1 / 0.2`，保留 `keep_structural_tokens=true`。  
3. 若 `pred_nan_rate` 仍接近 1.0，再引入 `true_weight>1.0`（例如 1.5）增强可答字段梯度。  
4. 每个试验都固定同一 val 集，记录 `macro_f1 / micro_f1 / pred_nan_rate / per-label F1` 后再决定是否全量训练。

## 6. V4 定义与实测复盘（已训练）
### 6.1 V4 的核心定义（和 V3 的区别）
- V3：主要是 `state-aware token weighting`，通过调高 `present/absent/uncertain` 权重来抑制 `null` 塌缩。
- V4：在 V3 基础上，增加 **answerability 掩码**，对 `answerability=false` 的 findings token 直接做 loss 降权或屏蔽（mask）。
- 目标：不改变 UMS 语义（`blank -> unknown/null`），但避免 70% 不可答字段主导梯度。

### 6.2 为什么把它作为 V4
- 当前瓶颈不是 JSON 生成失败，而是状态判别被稀释（`json_success_rate` 高但 `macro_f1` 低）。
- 仅靠 token weighting 仍可能被“结构模板 token”稀释；V4 进一步把不可答字段从主训练信号中移走。
- 相比“全量人工补标”，V4 成本更低、可先验证是否能快速提升分类有效监督。

### 6.3 V4 的训练口径（必须固定）
- **严格 UMS 口径（主结果）**：`blank/NaN -> unknown`，评估时对 `y_true` 的 NaN 位置做 mask。
- **baseline 对比口径（附录结果）**：额外报告 `blank->absent` 指标，仅用于和常见分类基线公平对比。
- 两套口径要并列报告，防止“指标看起来提升但语义口径变化”造成误判。

### 6.4 V4 的工程落地内容（已完成）
- 训练损失：在 `StructuredLoss`/trainer 中引入 answerability mask 的 token 级权重。
- 配置项：新增 `answerability_mask.enabled / false_weight / true_weight / scope`。
- 实验对比：至少给出 `V3` vs `V4` 的同划分对比（macro/micro F1、pred_nan_rate、per-label F1）。

### 6.5 V4 配套数据策略（与训练一起执行）
- teacher 伪标签：只对 `blank` 位置做高置信度填充，低置信度仍保持 unknown。
- calibration 人工标注：抽 200-500 张高 missing 标签样本，用于校准伪标签阈值。
- 标注产物：`annotation_queue.csv` + `manual_labels.csv`（写入 `.gitignore`，不入库）。

### 6.6 V4 成功判据（汇报时可直接用）
- 保持 `json_success_rate` 不下降（或下降不明显）。
- `macro_f1` 相对 V3 稳定提升，且 `pred_nan_rate` 明显下降。
- 至少 4 个高 missing 标签中，有 2 个以上标签 F1 出现可复现提升。

### 6.7 V4 实测结果（2026-02-09）
- 配置：`configs/cxr_chexpert_v4.yaml`（`answerability_mask.false_weight=0.0`，`scope=findings_only`）。
- 训练产物：`outputs/cxr_chexpert_v4/checkpoints/final.pt`、`outputs/cxr_chexpert_v4/checkpoints/best.pt`。
- Full eval：`outputs/cxr_chexpert_v4/checkpoints/best.metrics.full.json`。
- 指标：`json_success_rate=1.0`，`pred_nan_rate=1.0`，`macro_f1=0.0`，`micro_f1=0.2091`。
- 现象：14 个标签的 `per-label f1` 全为 0，模型生成格式正确但状态判别完全失效。
- 结论：V4 当前参数是“可运行但不可用”，需要进入 `V4.1` 参数修复而不是继续重复全量训练。

## 7. V4.1 计划（下一轮）
1. `false_weight` 从 `0.0` 上调到 `0.1`（必要时试 `0.2`），避免不可答字段完全无梯度。  
2. 保持 `state-aware token weighting` 不变，先只动一个变量，确保归因清晰。  
3. 先跑短程试验并快速评估，再决定是否跑 10k 全量。  
4. 若仍失败，再进入“伪标签 + calibration 标注”路线，而不是盲目加长训练。
## V4.2 临时补充（2026-02-11）
- 复评估文件：`outputs/cxr_chexpert_v4_2_dense10k/checkpoints/best.metrics.json`

## 8. V4.2 dense10k 结果记录（2026-02-11）
- 训练配置：`configs/cxr_chexpert_v4_2_dense10k.yaml`
- 训练数据：CheXpert train 的 dense top-10k（14 字段）；验证集：`data/dataset/processed/chexpert_ums_val.jsonl`（1000 条）
- 训练完成：`10000/10000`；模型产物：`outputs/cxr_chexpert_v4_2_dense10k/checkpoints/final.pt`
- 最优点：`val_loss=0.0522 @ step=3500`；后续在 `0.053~0.057` 区间波动
- 复评估（GPU1，`best.pt`）：`json_success_rate=1.0`、`pred_nan_rate=0.5937`、`micro_f1=0.5809`、`macro_f1=0.2553`

### 8.1 高分两项的数据覆盖率（非 NaN）
- 高分两项：`Lung Opacity`、`Support Devices`
- dense10k 训练子集（answerable=true 占比）：
- `Lung Opacity`: `73.34%`
- `Support Devices`: `70.12%`
- 本次验证集 1000（answerable=true 占比）：
- `Lung Opacity`: `53.80%`
- `Support Devices`: `56.80%`
- 结论：这两项不是“没数据”，相对是覆盖率更高的头部字段

### 8.2 长尾是否出现
- 已出现，而且明显
- 在 `v4.2` 的 14 标签里，`F1=0` 的标签有 7 个：
- `No Finding`、`Lung Lesion`、`Edema`、`Pneumonia`、`Pneumothorax`、`Pleural Other`、`Fracture`
- 验证集低覆盖示例：`Pleural Other=6.9%`、`No Finding=9.3%`、`Fracture=9.8%`、`Lung Lesion=10.8%`
- 结论：当前是“头部标签可学，长尾标签塌缩”的典型分布问题

### 8.3 对你两个问题的直接回答
- 问题1（是否因为没数据）：不完全是“没数据”；头部标签覆盖足够，因此能学起来；但长尾标签有效监督过少，导致整体 `macro_f1` 被拖低
- 问题2（长尾是否出现）：已出现，且已经影响到 v4.2 的主要瓶颈

## 9. V4.2 dense10k clean summary (2026-02-11)
- config: `configs/cxr_chexpert_v4_2_dense10k.yaml`
- train: CheXpert dense top-10k (14 labels), val: `chexpert_ums_val.jsonl` (1000)
- training finished: `10000/10000`
- best val loss: `0.0522 @ step 3500`
- eval file: `outputs/cxr_chexpert_v4_2_dense10k/checkpoints/best.metrics.json`
- eval metrics: `json_success_rate=1.0`, `pred_nan_rate=0.5937`, `micro_f1=0.5809`, `macro_f1=0.2553`

### 9.1 non-NaN coverage for the two high-F1 labels
- labels: `Lung Opacity`, `Support Devices`
- dense10k coverage (answerable=true):
- `Lung Opacity`: `73.34%`
- `Support Devices`: `70.12%`
- val(1000) coverage (answerable=true):
- `Lung Opacity`: `53.80%`
- `Support Devices`: `56.80%`

### 9.2 long-tail evidence in v4.2
- zero-F1 labels (7/14):
- `No Finding`, `Lung Lesion`, `Edema`, `Pneumonia`, `Pneumothorax`, `Pleural Other`, `Fracture`
- very low val coverage examples:
- `Pleural Other=6.9%`, `No Finding=9.3%`, `Fracture=9.8%`, `Lung Lesion=10.8%`
- conclusion: head labels are learnable, long-tail labels are still collapsing

## 10. V6 Top7 双实验补录（2026-02-12）
### 10.1 训练一眼结果（补录）
- `GPU0` 主实验：`configs/cxr_chexpert_v6_top7_gpu0.yaml`
- 训练完成：`4500/4500`，最优 `val_loss=0.0706 @ step 2400`，最终 `val_loss=0.0755 @ step 4500`
- 产物：`outputs/cxr_chexpert_v6_top7_gpu0_run1/checkpoints/best.pt`、`outputs/cxr_chexpert_v6_top7_gpu0_run1/checkpoints/final.pt`
- `GPU1` 抗塌缩对照：`configs/cxr_chexpert_v6_top7_gpu1_abstain.yaml`
- 训练完成：`1200/1200`，最优 `val_loss=0.0918 @ step 1080`，最终 `val_loss=0.0928 @ step 1200`
- 产物：`outputs/cxr_chexpert_v6_top7_gpu1_abstain_run1/checkpoints/best.pt`、`outputs/cxr_chexpert_v6_top7_gpu1_abstain_run1/checkpoints/final.pt`

### 10.2 评估结果（1000 样本）
| 实验 | 配置 | json_success_rate | pred_nan_rate | pred_unknown_policy | eval_coverage | macro_f1 | micro_f1 |
|---|---|---:|---:|---|---:|---:|---:|
| V6 Top7 GPU0 Run1 | `configs/cxr_chexpert_v6_top7_gpu0.yaml` | 1.0 | 0.3814 | `negative` | 1.0000 | 0.4087 | 0.5996 |
| V6 Top7 GPU1 Abstain Run1 | `configs/cxr_chexpert_v6_top7_gpu1_abstain.yaml` | 1.0 | 0.2526 | `abstain` | 0.4962 | 0.6357 | 0.8806 |

### 10.3 指标解读（避免误读）
- 两条线都已脱离 `pred_nan_rate=1.0` 的全空塌缩阶段（相较 V4 是实质进展）。
- `GPU0` 是“强制给结论”（`pred_unknown_policy=negative`，覆盖率 100%），更接近上线推理场景。
- `GPU1` 是“允许弃答”（`pred_unknown_policy=abstain`），覆盖率仅 49.62%，`micro_f1/macro_f1` 不可与 `GPU0` 直接横向比较。
- `GPU0` 里 `Pleural Effusion/Edema/Pneumothorax` 仍有“高精度-低召回”问题（预测偏保守）。
- `GPU1` 里部分标签 support 极小（如 `Edema=2`、`Consolidation=13`、`Atelectasis=33`），说明该策略在评估时筛掉了大量难样本。

### 10.4 直接结论
- 当前可作为主线继续扩展的是 `GPU0` 这条（全覆盖、可对比、无弃答逃逸）。
- `GPU1 abstain` 更适合作为诊断工具，而不是主结果。

## 11. V5 Top3 历史实验补录（2026-02-12 追记）
### 11.1 V5 pilot（GPU1, 120 steps）
- 配置：`configs/cxr_chexpert_v5_top3_gpu1_pilot.yaml`
- 字段：`Pleural Effusion`、`Lung Opacity`、`Support Devices`
- 指标文件：`outputs/cxr_chexpert_v5_top3_gpu1_pilot_run1/checkpoints/best.metrics.json`
- 指标：`json_success_rate=1.0`、`pred_nan_rate=0.4880`、`macro_f1=0.5712`、`micro_f1=0.5633`
- 说明：该 pilot 主要用于“是否能快速脱离全空塌缩”的机制验证，不作为最终主结论。

### 11.2 V5.1（GPU0, 400 steps）
- 配置：`configs/cxr_chexpert_v5_1_top3_gpu0_pilot.yaml`
- 指标文件：`outputs/cxr_chexpert_v5_1_top3_gpu0_run1/checkpoints/best.metrics.json`
- 指标：`json_success_rate=1.0`、`pred_nan_rate=0.2020`、`macro_f1=0.8909`、`micro_f1=0.8518`
- per-label 关键观察：
- `Pleural Effusion`：`precision=0.9490`、`recall=0.5975`、`f1=0.7333`（高精度低召回）
- `Lung Opacity`：`f1=0.9677`
- `Support Devices`：`f1=0.9718`
- 结论：Top3 聚焦可显著降低塌缩，但字段间学习难度仍明显不均衡（尤其 `Pleural Effusion` 召回偏低）。

## 12. 是否超过 baseline（截至当前）
### 12.1 对比口径说明
- 当前可直接使用的 baseline 文件：`outputs/baseline_vit_v2/best.metrics.json`
- 该 baseline 是纯分类 ViT（非生成），与 VIVID 任务形态不同，解释时需要强调“上界参照”属性。

### 12.2 结论
- **结论：当前尚未超过 baseline。**
- 全 14 字段口径：VIVID（最佳阶段）仍显著低于 `baseline_vit_v2`（`macro_f1=0.9125`、`micro_f1=0.8948`）。
- Top7 主线（V6 GPU0）：`macro_f1=0.4087`、`micro_f1=0.5996`，与 baseline 的同字段估算上界仍有明显差距。
- Top3（V5.1 GPU0）：`macro_f1=0.8909`、`micro_f1=0.8518`，已接近但尚未超过 baseline 的同类头部字段表现。

### 12.3 为什么还没超过（技术原因）
1. 目标函数效率差异：baseline 直接分类监督；VIVID 先生成结构文本再解析，梯度更稀释。
2. 数据分布问题：`blank/NaN` 与可答字段不均衡仍在，导致部分字段长期“保守预测”。
3. 指标口径差异：`abstain` 可提升表面 F1，但降低覆盖率，不能当作真正超越依据。
4. 字段难度不均：以 `Pleural Effusion` 为代表，当前主要瓶颈是召回不足而非精度不足。

### 12.4 当前阶段判断
- 已完成“脱离全空塌缩”目标；
- 正处在“提升覆盖下的稳定召回”阶段；
- 距离“稳定超过 baseline”还需要至少一轮结构化优化（不仅是延长训练步数）。

## 13. Baseline Top7（dense10k）本轮实跑结果（2026-02-12）
### 13.1 训练完成状态
- 配置：`configs/baseline_vit_chexpert_top7_dense10k.yaml`
- 目录：`outputs/baseline_vit_top7_dense10k`
- 训练完成：`4500/4500`，最终 `Step 4500: val_loss = 0.2686`
- 关键产物：`best.pt`、`final.pt`、`metrics_final.json`

### 13.2 标准评估（best.pt）
- 评估命令：`scripts/eval_vit_baseline.py`（同配置、同 val1k）
- 指标文件：`outputs/baseline_vit_top7_dense10k/best.metrics.json`
- 结果：`macro_f1=0.8825`，`micro_f1=0.8956`
- per-label 关键值：
- `Pleural Effusion`：`F1=0.9397`（`P=0.9363`, `R=0.9432`）
- `Support Devices`：`F1=0.9838`
- `Lung Opacity`：`F1=0.9770`
- `Edema`：`F1=0.8921`
- `Pneumothorax`：`F1=0.5299`（当前 baseline 里最难标签）
- `Consolidation`：`F1=0.8618`
- `Atelectasis`：`F1=0.9934`

### 13.3 与当前 VIVID 主线对比（同 Top7）
- VIVID 主线：`outputs/cxr_chexpert_v6_top7_gpu0_run1/checkpoints/best.metrics.json`
- 指标：`macro_f1=0.4087`，`micro_f1=0.5996`（`eval_coverage=1.0`）
- 结论：当前 VIVID 仍明显低于同口径 baseline，尚未实现超越。

## 14. 2026-02-12 追加记录：Baseline重跑 vs V7/V7.1

### 14.1 指标文件
- Baseline：`outputs/baseline_vit_top7_dense10k_rerun/metrics_final.json`
- V7（GPU0）：`outputs/cxr_chexpert_v7_top7_gpu0_run2/checkpoints/best.metrics.json`
- V7.1（GPU1）：`outputs/cxr_chexpert_v7_1_lowrecall_gpu1_run1/checkpoints/best.metrics.json`

### 14.2 汇总指标（同Top7，val=1000）
| 实验 | macro_f1 | micro_f1 | 备注 |
|---|---:|---:|---|
| Baseline rerun | 0.8839 | 0.8974 | 纯ViT分类（BCE） |
| V7 GPU0 run2 | 0.5115 | 0.7051 | `pred_nan_rate=0.1786` |
| V7.1 GPU1 run1 | 0.4617 | 0.6686 | `pred_nan_rate=0.0003` |

### 14.3 相对 Baseline 的差值
- V7 GPU0 run2：`macro_f1 -0.3724`，`micro_f1 -0.1923`
- V7.1 GPU1 run1：`macro_f1 -0.4222`，`micro_f1 -0.2288`

### 14.4 结论
- 本轮结论：V7 与 V7.1 均未超过最新 baseline。
- 主短板仍是低召回标签（尤其 `Edema/Pneumothorax/Consolidation`）。

## 15. 2026-02-12 追加：同头公平对比结论（用于主表）

### 15.1 同头对比结果
- Baseline rerun：`outputs/baseline_vit_top7_dense10k_rerun/metrics_final.json`
  - `macro_f1=0.8839`, `micro_f1=0.8974`
- From-VIVID：`outputs/baseline_vit_top7_dense10k_from_vivid/metrics_final.json`
  - `macro_f1=0.8857`, `micro_f1=0.8984`

### 15.2 相对提升
- From-VIVID vs Baseline rerun：
  - `macro_f1 +0.0018`
  - `micro_f1 +0.0011`

### 15.3 结论
- 在“同一分类头 + 同一数据口径”的公平协议下，From-VIVID 已小幅超过 Baseline。
- 该结论优先级高于“生成链路直接对比分类链路”的结果。

## 16. 2026-02-12 追加：V7.1 低于 V7 的原因

### 16.1 指标差值
- V7：`macro_f1=0.5115`, `micro_f1=0.7051`
- V7.1：`macro_f1=0.4617`, `micro_f1=0.6686`
- 差值：`macro -0.0498`, `micro -0.0365`

### 16.2 原因归纳
- V7.1 的采样与权重设定更激进（固定 `K=3`、`focus_probability=1.0`、低召回标签更高权重）。
- 训练分布与全量验证分布产生偏移，导致泛化下降。
- 训练曲线显示早期最优后退化（best `val_loss=0.1879@step400`，末期 `0.2271@step3000`）。

## 17. 代码层面问题审查（2026-02-12）

### 17.1 已定义但从未启用的损失函数

- `training/losses.py` 中定义了 `RankingLoss`（L_rank）、`VisualDependenceLoss`（L_vdep）、`AnswerabilityLoss`（L_ans）。
- 但所有实验配置中 `lambda_rank=0.0`、`lambda_vdep=0.0`、`lambda_ans=0.0`，这三个损失从未实际参与训练。
- 文档 02/07 中描述的 hard negative ranking、anti-shortcut 正则、answerability head 均未落地。

### 17.2 Constrained Decoding 未实现

- 文档 02 明确要求训练时使用 Constraint-aware Teacher Forcing（masked-CE），推理时使用 constrained decoding。
- 实际代码 `models/vivid_model.py` 的 `generate()` 方法使用的是标准 HuggingFace `generate()` + 事后 heuristic JSON 提取（括号匹配 + `json.loads`），没有 grammar/FSM/trie 约束。
- 训练时也没有对非法 token 的 logits 做 `-∞` 屏蔽。

### 17.3 Hard Negative 构造未实现

- 文档 02 要求每条样本构造 3-5 个 hard negative JSON（laterality flip、unit swap、measurement ×10 等）。
- 代码中没有任何 hard negative 构造逻辑。

### 17.4 评估代码已实现但未被调用

- `evaluation/metrics.py` 中 `compute_risk_coverage_curve()` 和 `compute_clinical_hard_errors()` 已完整实现。
- `evaluation/metrics.py` 中 `compute_reliability_metrics()`（ECE/Brier）已完整实现。
- 但在 `scripts/eval_vivid.py` 和 `scripts/eval_vit_baseline.py` 中均未调用这些函数。
- 当前评估只报告 F1/precision/recall，缺少 AUC、ECE、risk-coverage 等 MICCAI 标配指标。

### 17.5 训练策略过度工程化

- V7 配置同时启用 4 个独立机制：`field_query_training`、`token_weighting`、`answerability_mask`、`label-specific weights`。
- 这些机制之间存在交互效应，V7.1 的退化（`macro_f1` 从 0.5115 降到 0.4617）说明参数空间过大、调参方向不可控。
- 建议：先固定一个简洁的 baseline 配置，逐个启用机制并做消融，而不是同时叠加。

## 18. 2026-02-12 追加：Route B 分组学习率同头对比

### 18.1 配置
- Baseline RouteB：`configs/baseline_vit_chexpert_top7_dense10k_routeb_rerun.yaml`
- From-VIVID RouteB：`configs/baseline_vit_chexpert_top7_dense10k_routeb_from_vivid.yaml`
- 超参：`backbone_lr=1e-5`, `head_lr=1e-4`, `wd=0.01`, `warmup=400`, `max_steps=4500`

### 18.2 最终结果
- Baseline RouteB：`macro_f1=0.8722`, `micro_f1=0.8984`
- From-VIVID RouteB：`macro_f1=0.8864`, `micro_f1=0.9006`

### 18.3 相对提升（From-VIVID - Baseline）
- `macro_f1 +0.0143`
- `micro_f1 +0.0022`

### 18.4 结论
- Route B 下，同头公平对比优势扩大，尤其 `macro_f1` 明显提升。
- 4500步仍有后期抹平趋势，但最终差距保持为正。

### 18.5 口径差异补充（final vs best checkpoint）
- `metrics_final`：From-VIVID 相对 Baseline 为 `macro +0.0143`, `micro +0.0022`。
- `best.pt`（按 val_loss 选）：From-VIVID 相对 Baseline 为 `macro -0.0047`, `micro +0.0022`。
- 结论：当前存在"优化目标(loss)与汇报目标(macro_f1)错位"，后续需要对齐早停/选模指标。

## 19. V8 设计记录（2026-02-12）

### 19.1 问题诊断

V7 的 From-VIVID transfer 仅比 ImageNet baseline 高 +0.0018 macro_f1，根本原因：

1. **CLS-only 信息瓶颈**：`vit_output_type="cls"` 只输出 1 个 768 维 token，丢弃了 196 个 patch 的空间信息
2. **Projector 过强**：MLP hidden=3072 + 16 prefix tokens（~7M 参数），足以在不改变 ViT 的情况下拟合 LLM 输入
3. **单一学习率**：ViT 和 Projector 共用 lr=8e-5，Projector 优先收敛，ViT 梯度被稀释
4. **训练步数不足**：3500 步 × effective batch 32 = 112K 样本，对 ~50% null 的数据集有效训练量偏少

### 19.2 V8 核心改动（vs V7）

| 项目 | V7 | V8 | 理由 |
|---|---|---|---|
| vit_output_type | cls (1 token) | all (197 tokens) | 迫使 ViT 在 patch 级别编码信息 |
| num_prefix_tokens | 16 | 4 | 减少 Projector 的"免费"容量 |
| projector_mlp_hidden_dim | 3072 (默认) | 1536 | 削弱 Projector，迫使 ViT 承担更多 |
| 学习率 | 统一 8e-5 | ViT=2e-5, Proj=1e-4 | 差异化：ViT 缓慢调整，Projector 快速适配 |
| max_steps | 3500 | 5000 | 更多有效梯度更新 |

### 19.3 配置文件

- VIVID 预训练：`configs/cxr_chexpert_v8_top7_gpu0.yaml`
- Transfer 评估：`configs/baseline_vit_chexpert_top7_dense10k_from_vivid_v8.yaml`
  - backbone_lr=1e-5（保护 VIVID 表征），head_lr=1e-3（新分类头快速收敛）

### 19.4 代码改动（向后兼容）

- `models/vivid_model.py`：新增 `projector_mlp_hidden_dim` 参数 + `get_trainable_parameter_groups()` 方法
- `training/trainer.py`：新增 `vit_learning_rate` / `projector_learning_rate` 参数，支持分组优化器
- `scripts/train_cxr.py`：透传新参数到模型和 trainer

### 19.5 数据集

- 训练集：dense10k（`train_dense_top_k: 10000`），与 V7 一致
- 验证集：1000 样本（`val_dense_top_k: 1000`）

### 19.6 预期结果 vs 实际结果

**预期**：Transfer macro_f1 从 0.8857 提升到 0.89~0.90 区间

**实际（2026-02-13）**：

| 实验 | macro_f1 | micro_f1 | macro_auc | vs Baseline |
|---|---:|---:|---:|---|
| Baseline (ImageNet) | 0.8839 | 0.8974 | — | — |
| From-VIVID V7 | 0.8857 | 0.8984 | — | +0.0018 / +0.0010 |
| From-VIVID V8 (best@step2700) | 0.8852 | 0.9006 | — | +0.0013 / +0.0032 |
| From-VIVID V8 (final@step4500) | 0.8790 | 0.8963 | 0.8475 | -0.0049 / -0.0011 |

**V8 per-label F1（final）**：

| Label | F1 | AUC | Recall |
|---|---:|---:|---:|
| Pleural Effusion | 0.9372 | 0.9636 | 0.9210 |
| Support Devices | 0.9838 | 0.5921 | 1.0000 |
| Lung Opacity | 0.9635 | 0.8472 | 0.9714 |
| Edema | 0.8932 | 0.8756 | 0.8894 |
| Pneumothorax | 0.5222 | 0.7607 | 0.4046 |
| Consolidation | 0.8596 | 0.9361 | 0.8417 |
| Atelectasis | 0.9934 | 0.9574 | 1.0000 |

**结论**：V8 的 4 项改动（all tokens + 削弱 Projector + 分组 lr + 更多步数）未带来预期提升。best step macro_f1=0.8852 与 V7 持平，final 阶段出现过拟合下降。micro_f1 在 best step 有小幅提升（+0.0032）。Pneumothorax 仍是最弱标签（F1=0.52, recall=0.40）。

### 19.7 运行命令

```bash
# Step 1: V8 VIVID 预训练
python scripts/train_cxr.py --config configs/cxr_chexpert_v8_top7_gpu0.yaml

# Step 2: V8 Transfer 评估
python scripts/train_vit_baseline.py --config configs/baseline_vit_chexpert_top7_dense10k_from_vivid_v8.yaml
```

### 19.8 状态

- [x] VIVID 预训练（已完成，GPU0）
- [x] Transfer 评估（已完成，结果未达预期，macro_f1=0.8852 与 V7 持平）
- [ ] V8-lite 对照实验（GPU1 运行中）
