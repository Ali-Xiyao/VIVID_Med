# 训练结果记录

## 2026-02-04 — VIVID CXR 训练（CheXpert）

**运行命令**
```bash
python .\scripts\train_cxr.py --config .\configs\cxr_chexpert.yaml
```

**数据与划分**
- 数据集：`data/dataset/processed/chexpert_ums.jsonl`
- 总样本：30,000
- 训练集：29,000（batch_size=8，drop_last=True，3625 batches）
- 验证集：1,000（从训练 JSONL 中划分）

**模型配置**
- ViT：`vit_base_patch16_224`（pretrained）
- LLM：`Qwen/Qwen2.5-1.5B-Instruct`（冻结）
- Prefix tokens：16

**训练超参（核心）**
- max_steps：10,000
- learning_rate：1e-4
- weight_decay：0.01
- warmup_steps：500
- batch_size：8
- gradient_accumulation_steps：4（有效 batch = 32）
- bf16：true
- eval_interval：500
- save_interval：1000

**结果**
- 最佳验证集 loss：**0.0320**
- 终止时 step：10,000
- Checkpoints：`outputs/cxr_chexpert/checkpoints/`  
  - `best.pt`, `final.pt`, `step_1000.pt` … `step_10000.pt`

**备注**
- 未安装 `flash_attn`，自动回退到 eager attention（正常）。
- `torch_dtype` deprecation 为提示信息，不影响训练。

**评估（生成 UMS JSON）**
- 评估命令：`python .\scripts\eval_vivid.py --config .\configs\cxr_chexpert.yaml --checkpoint .\outputs\cxr_chexpert\checkpoints\best.pt`
- 输出文件：`outputs/cxr_chexpert/checkpoints/best.metrics.json`
- 结果摘要：
  - `num_samples` = 1000
  - `json_success_rate` = 0.0
  - `pred_nan_rate` = 1.0
  - `macro_f1` = 0.0
  - `macro_auc` = null
- 结论：当前生成结果无法解析为 JSON（成功率为 0），指标无效，需要先检查生成文本输出。

**样例输出检查（2026-02-04）**
- 命令：`python .\scripts\sample_vivid_outputs.py --config .\configs\cxr_chexpert.yaml --checkpoint .\outputs\cxr_chexpert\checkpoints\best.pt --num_samples 5`
- 现象：
  - 生成文本普遍以 `}, "Lung Lesion": ...` 之类的片段开头，缺少顶层 `{ ... }` 前缀
  - 文本末尾出现 `Human:` 等对话残留
  - 5/5 样本 `parsed_json: FAILED`
- 初步判断：生成格式与期望 JSON 不匹配，需调整 prompt 或解析策略后再评估

**补充分析（2026-02-04）**
- 生成文本普遍缺少 JSON 起始 `{`，且夹带大量“Human/Assistant”对话残留。
- 代码问题：`generate()` 解码时只跳过了 prompt tokens，没有跳过视觉 tokens，导致输出从中间截断。
- 修复点：在 `models/vivid_model.py` 中将解码起始索引改为 `num_visual_tokens + prompt_len`。
