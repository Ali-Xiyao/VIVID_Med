# Task Plan

## Goal

Run one separately locked, fresh-development, three-seed replication of the
unchanged VIVID-GDS schema bridge against A0 direct schema and A2 UMS prefix4,
without reusing the terminal Stage-A surface for selection.

## Phases

- [x] Receive explicit approval to open a separate replication protocol.
- [x] Audit candidate fresh-development data, overlap, provenance, labels, and
  server availability.
- [x] Freeze data identity, A0/A2/A3 identities, three-seed budget, checkpoint
  rules, statistics, and survival thresholds.
- [x] Implement a fail-closed readiness audit and replication queue.
- [x] Validate locally and on the server without opening the new scores.
- [ ] Launch the smallest authorized three-seed survival gate.
- [ ] Freeze PASS or NO-GO and update GitHub.

## Next Step

Commit and push the locked protocol/code, synchronize the final source state,
then launch the sequential allocation-3066 queue.

## Boundaries

- The terminal VIVID-GDS Stage-A run and CheXpert expert-development scores
  remain frozen history.
- Do not tune `lambda_schema`, teacher size, projector, targets, or thresholds.
- Do not use CheXlocalize test, VinDr test, or historically exposed surfaces
  as fresh selection evidence.
- Do not copy medical images, patient rows, predictions, checkpoints, or
  caches into Git.

## Errors Encountered

| Error | Attempt | Resolution |
|---|---:|---|
